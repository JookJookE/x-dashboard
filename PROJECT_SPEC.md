# 📜 X Content Helper - 프로젝트 명세서 및 버전 관리 문서 (PROJECT_SPEC.md)

> **⚠️ 필독 지침 (AGENT DIRECTIVE)**  
> 작업 전 반드시 이 문서를 확인하고, 기술된 **수집 정책, 프롬프트 모드, 시스템 제약 사항**을 엄격히 준수하십시오.  
> 사용자의 명시적 승인 없이 작성된 수집 범위(`2h`, `5d`), 크론 주기(`0 * * * *`), 스레드 분리 방식 등을 임의로 수정하는 것은 금지됩니다.


---

## 📌 1. 프로젝트 개요 & 시스템 아키텍처

- **프로젝트명**: X Content Helper (하이젠버그, 테크/금융, 커뮤니티 AI 트윗 생성기)
- **배포 환경**: 로컬 환경 (Mini PC / 노트북)
- **핵심 역할**: 국내외 뉴스, 블로그, 커뮤니티 썰을 자동/수동 수집하고 Gemini AI를 통해 X(트위터) 고도화 모드별 트윗 초안 생성

---

## ⚙️ 2. 핵심 수집 및 보관 정책 (STRICT RULES)

| 항목 | 정책 내용 | 비고 |
| :--- | :--- | :--- |
| **자동 수집 주기** | **매시간 정각 (`0 * * * *`)** 1시간 간격 자동 실행 (DB 보관) | `scheduler.js` (cron) |
| **텔레그램 알림 주기** | **10분 주기 (`*/10 * * * *`)** 실시간 핫이슈/스몰톡 1건 발송 (07:00~23:59 KST) | `telegramQueueService.js` (00~07시 야간 정적 모드) |
| **수동 수집** | 대시보드 `[새로고침 / 수집]` 버튼으로 언제든지 즉시 수동 실행 | UI 트리거 가능 |
| **기사 DB 보관 기간** | **5일 (`5d`)** 보관 후 자동 정리 | `history.js` 기준 |
| **테크 & 금융 수집 범위** | **최근 2시간 이내 (`when:2h`)** 기사만 수집 | IT, 코인, 주식, 경제, 글로벌 외신 |
| **커뮤니티 수집 범위** | **최근 5일 이내 (`when:5d`)** 게시글 수집 | 블라인드, 네이트판, 가십, 멘탈/심리 |

---

## 🤖 3. 트윗 작성 프롬프트 모드 (버전 1 ~ 버전 7)

1. **⚡ 버전 1: 이모지 요약 (심층 팩트 & 시장 분석형)**  
   - 핵심 팩트 3줄 압축 이모지 요약 + 전문가 관점의 구조적 시장/산업 심층 분석 & 시사점 도출
2. **🔥 버전 2: 아티클 (1인칭 뷰)**  
   - 1인칭 분석 리포트형 (1줄 도파민 제목 + 1인칭 통찰 본문)
3. **💬 버전 3: 짧은 리액션 (1~2줄 썰)**  
   - 기사를 읽고 친구나 커뮤니티에 방금 던지는 듯한 초단문 리얼 리액션 (예: "와 ~라는데 진짜 ~한 거 같지 않아?")
4. **🧠 버전 4: 멘탈/공감 꿀팁형**  
   - 공감, 심리학, 대인관계 꿀팁 전달
5. **🌟 버전 5: 하이브리드 후킹 (전문가 뷰)**  
   - 팩트 + 전문가의 단호한 인사이트 결론 (타래 및 외부 링크 유도 금지)
6. **🌶️ 버전 6: 매운맛 꺼드럭 썰 (스파이시 리액션)**  
   - 유쾌하게 비꼬거나 본인을 치켜세우며 날것의 표현과 인터넷 밈/비속어 초성(sbg, ㄷㄷ 등)을 섞어 쓰는 매운맛 트위터리안 스타일
7. **💬 엑친 소통 / 스몰톡 뻘글 모드 (신규)**  
   - 핫이슈 부재 또는 중복 시, 엑친들에게 수다를 떨거나 질문/밸런스 게임/일상 공감을 던지는 답글 유발형 스몰톡 창작

---

## 🌐 4. 네트워크 및 환경 제약 사항

1. **구글 뉴스 RSS 및 Bing 뉴스 RSS 통신**  
   - 구글 데이터센터 IP 차단 정책이나 일시적인 네트워크 오류로 직접 접속 시 차단(403/Timeout)될 수 있음.
   - 1차 구글 직통 실패 시 **2차 Bing News RSS 백업 엔진**으로 자동 전환하여 실시간 기사를 100% 수집.
   - 3차 최종 백업으로 `rss2json` 우회 API 유지.
2. **병렬 통신 (`Promise.all`) 필수**  
   - 응답 속도 향상 및 대기 시간 최소화를 위해 반드시 `Promise.all`을 이용해 초고속 병렬 처리 완료할 것.
3. **실시간 오류 / IP 차단 로그 기록 (`addLog`)**  
   - HTTP 403 / 429 발생 시: `🚨 [IP 차단/제한 발생]` 시스템 로그 기록
   - 타임아웃 발생 시: `⏱️ [타임아웃 발생]` 시스템 로그 기록
   - RSS 응답 실패 시: `⚠️ [수집 차단/오류]` 시스템 로그 기록

---

## 📅 6. 버전 변경 이력 (Changelog)

### v3.10.2 (2026-08-20) - 🧵 2단 타래(스레드) 전용 듀얼 텍스트창 UI 및 자동 채움/클립보드 연동 개편
- 📝 **1번/2번 듀얼 텍스트창 분할 (`public/app.js`, `public/index.html`)**:
  - `[🧵 9. 2단 타래(스레드)]` 선택 시 단일 텍스트창이 1번(훅) / 2번(인사이트) 독립 텍스트창으로 자동 전환.
- ⚡ **1-Click 스마트 분할 포스팅 워크플로우**:
  - `[🌐 X 작성창 열기]` 클릭 시 **1번 글은 X 작성창에 자동 입력되어 열리고**, **2번 글은 클립보드에 자동 복사**됨.
  - X에서 1번 글 [게시하기] 후 바로 [답글]에 `Ctrl+V` (붙여넣기) 하면 1초 만에 2단 스레드 완성.

### v3.10.1 (2026-08-20) - 🟩 웹 대시보드 및 텔레그램 포스팅 시 실시간 스트릭(잔디) 100% 자동 집계 연동
- ⚡ **웹 대시보드 포스팅 집계 누락 해결 (`public/app.js`, `history.js`)**:
  - `🌐 X 작성창 열기`, `📰 X 아티클 에디터 열기` 실행 시 기사 선택 여부(자유 작성/스몰톡 포함)와 무관하게 즉시 백엔드 포스팅 기록 및 잔디밭 집계(`loadStreakStats`)가 실시간 반영되도록 수정.
  - `markPostingStatus` 내부에 `incrementTodayXPostCount()`를 상시 호출하여 텔레그램 승인 및 웹 게시 모두 잔디밭에 100% 누적.

### v3.10.0 (2026-08-20) - 🚀 실전 바이럴 4대 킬러 기능 탑재 (타언론사 중복 100% 차단, 투표/스레드 생성기, 골든아워 뱃지, 30일 잔디밭 스트릭)
- 🛡️ **타 언론사 동일 내용 중복 100% 차단 & 🚨 초대형 긴급 속보 즉시 핑 (`services/telegramQueueService.js`, `scraper.js`)**:
  - 한국어 어미 조사 제거 및 어간 유사도 분석 강화로, 언론사가 달라도 최근 24시간 내 이미 나간 주제는 텔레그램에서 100% 필터링.
  - 단시간 3개 이상 언론사 동시 집중 보도 시 `🚨 [초긴급 속보 감지]` 태그 부착.
- 🗳️ **X 공식 투표(Poll) & 🧵 2단 타래(스레드) 자동 생성기 (`summarizer.js`, `public/app.js`)**:
  - 팔로워 참여 폭발용 A vs B 투표 질문 및 2~4개 선택지 생성(`generatePollTweet`).
  - 1편(호기심 훅+사진) + 2편(반전 결론/인사이트) 2단 스레드 세트 생성(`generateThreadTweet`).
- ⏰ **트위터 트래픽 피크 타임 '골든 아워(Golden Hour)' 실시간 뱃지**:
  - KST 기준 출근(07:30~09:00), 점심(11:50~13:00), 퇴근(18:00~19:30), 심야(22:30~24:00) 피크 시간대 자동 감지 및 대시보드 헤더/텔레그램 뱃지 표시.
- 📊 **대시보드 상단 '포스팅 스트릭(잔디 심기)' & 일일 목표 트래커 (`history.js`, `public/index.html`, `public/style.css`, `public/app.js`)**:
  - 깃허브 스타일의 최근 30일 포스팅 잔디밭(🟩) 컴포넌트 탑재 및 연속 달성 스트릭(`🔥 N일 연속 포스팅 달성 중!`) 실시간 연동.

### v3.9.11 (2026-08-20) - 🛡️ 밀린 과거 텔레그램 메시지 사진 앨범 100% 영구 삭제 보장 (DB 영구 기록 & 직전 ID 탐색 Fallback)
- 💾 **미디어 메시지 ID 영구 파일 DB 저장 (`services/telegramQueueService.js`)**:
  - 발송 즉시 `telegram_queue_status.json`에 3장의 사진 메시지 ID(`mediaMessageIds`)를 영구 저장하여 서버 재시작 후에도 정보 보존.
- 🎯 **인접 메시지 직전 ID 탐색 폴백 (Double-Safety Net)**:
  - 과거에 밀려있던 메시지의 버튼 클릭 시, 매핑 정보가 없더라도 텍스트 메시지의 직전 번호들(`messageId - 1`, `messageId - 2`, `messageId - 3`, `messageId - 4`)을 역추적하여 3종 캡처 사진 앨범을 100% 무조건 삭제.

### v3.9.10 (2026-08-20) - 🗣️ '엑친/트친' 작위적 호칭 전면 제거 및 자연스러운 일상 질문형 문장으로 전면 개편
- 🚫 **인위적 호칭 박멸 (`summarizer.js`)**:
  - `NEGATIVE_PROMPT_BLOCK`에 "엑친", "엑친들", "액친", "액친들", "트친", "트친들"을 금지어로 등록.
  - 모든 핫이슈 및 스몰톡 템플릿/프롬프트에서 '엑친' 단어를 전면 삭제하고, **"다들 어떻게 보시나요?", "여러분 생각은 어떠신가요?", "다들 체감은 어떤지 궁금하네요"** 등 자연스럽고 사람 냄새 나는 어조로 완전 대체.

### v3.9.9 (2026-08-20) - 📱 텔레그램 사진 앨범 스마트 정리 & 완료/스킵 텍스트 및 [🌐 𝕏 작성창 다시 열기] 보존
- 📸 **사진 앨범만 선별 삭제 (`services/telegramQueueService.js`)**:
  - `[✅ 𝕏 올림 완료]` 또는 `[🗑️ 건너뛰기]` 클릭 시, 스크롤을 많이 차지하는 3장의 캡처 사진 앨범만 깔끔하게 자동 삭제(`deleteMessage`).
- 📝 **기사 제목 & 트윗 멘트 & 다시 열기 버튼 보존**:
  - `[✅ 𝕏 올림 완료]` 시: 기사 제목 + 트윗 멘트 + `[🌐 𝕏 작성창 다시 열기 ↗]` 링크 버튼을 담은 완료 메시지로 압축 업데이트(`editMessageText`).
  - `[🗑️ 건너뛰기]` 시: `🗑️ [건너뜀] 해당 트윗 멘트는 스킵되었습니다.` 및 기사 제목 표기 상태로 압축 업데이트.

### v3.9.8 (2026-08-20) - 🧹 텔레그램 승인/스킵 시 사진 앨범 및 텍스트 메시지 자동 즉시 삭제 (스크롤 폭주 방지)
- 🗑️ **메시지 & 미디어 그룹 자동 삭제 파이프라인 (`services/telegramQueueService.js`)**:
  - 텔레그램에서 `[✅ 𝕏 올림 완료]` 또는 `[🗑️ 건너뛰기]` 버튼 클릭 시, 텔레그램 봇 API(`deleteMessage`)를 호출하여 해당 건의 **3종 캡처 사진 앨범 전체 + 트윗 멘트 텍스트 메시지를 채팅방에서 흔적 없이 즉시 자동 삭제**.
  - 완료 시 사용자에게 알림 토스트(`answerCallbackQuery`)를 띄우고 DB 상태(`markPostingStatus`)를 안전하게 업데이트.
  - ➔ 10분마다 전송되더라도 채팅방에 지난 사진과 멘트가 쌓이지 않아 스크롤이 항상 깔끔하게 유지됨.

### v3.9.7 (2026-08-20) - 🎨 대시보드 3종 캡처 카드([제목+사진], [제목만], [제목+본문]) 서버 사이드 100% 동일 렌더링 엔진 완성
- 🎯 **대시보드 캔버스 렌더링 규칙 1:1 완벽 이식 (`services/cardNewsServerService.js`)**:
  - 📸 **기사 캡처 (제목+사진)**: 대시보드와 동일한 사진 비율 가로 880px 비례 스케일, 라운드 코너(rx: 10), 실시간 고화질 사진 합성.
  - 📝 **기사 캡처 (제목만)**: 높이 `200 + (줄수 * 46px)` 규격, 상단 언론사 태그 + 32px Bold 헤드라인 + 기자명/날짜 + 구분선.
  - 📄 **기사 캡처 (제목+본문)**: 22px 본문 요약 줄바꿈 렌더링 (최대 18줄, 줄 간격 40px).
- 📱 **텔레그램 자동 연동**: 10분 주기 실시간 핫이슈 발송 시 대시보드에서 보던 3종 캡처 카드 그대로 텔레그램 앨범으로 자동 전송.

### v3.9.6 (2026-08-20) - 🔍 카드뉴스 실시간 고화질 원본 사진 추출 엔진 강화 (구글 로고 유입 원천 차단)
- 🎯 **4단계 심층 이미지 해결 엔진 탑재 (`services/cardNewsServerService.js - resolveArticleImageUrl`)**:
  - 1단계: `news.google.com` 링크의 실제 언론사 최종 목적지 URL 자동 추적.
  - 2단계: 실제 언론사 웹페이지 내 `og:image` 및 본문 대표 사진 정밀 추출.
  - 3단계: 구글 로고/아이콘(`google`, `gstatic`, `logo`, `spg` 등) 엄격 필터링.
  - 4단계: 네이버 뉴스 / 다음 뉴스 고화질 언론사 원본 사진 검색 백업 엔진 가동.
  - ➔ 구글 로고가 잘못 합성되던 문제를 100% 원천 차단하고, 웹 대시보드와 동일한 고화질 원본 기사 사진이 완벽하게 합성되도록 수정.

### v3.9.5 (2026-08-20) - 📸 텔레그램 3종 캡처 카드 앨범 동시 발송 탑재 ([제목+사진] / [제목+본문] / [제목만])
- 📸 **3종 캡처 렌더링 엔진 완비 (`services/cardNewsServerService.js`)**:
  - `generateCardNewsPhoto` (📸 기사 캡처: 제목+사진)
  - `generateCardNewsBody` (📝 기사 캡처: 제목+본문)
  - `generateCardNewsTitleOnly` (📄 기사 캡처: 제목만)
  - `generateAll3CaptureCards`: 3종 이미지를 초고속 병렬(`Promise.all`) 생성.
- 📱 **텔레그램 미디어 그룹(사진 앨범 묶음) 발송 연동 (`services/telegramQueueService.js`)**:
  - 10분 주기 실시간 핫이슈 발송 시 `sendMediaGroup`으로 3장의 캡처 카드를 1개의 앨범으로 묶어 전송하고, 바로 이어서 트윗 멘트 + 1-Click X 올리기 버튼 전송.
  - 사용자가 모바일 텔레그램에서 3종 카드 중 마음에 드는 이미지를 자유롭게 선택하여 활용 가능.

### v3.9.4 (2026-08-20) - 🤖 Gemini 최신 9종 모델 풀(`GEMINI_MODELS_POOL`) 전체 AI 함수 일원화
- 🚀 **공통 모델 풀 통합 (`summarizer.js`, `visualMediaService.js`)**:
  - `generateSummary`, `generateThoughtTweet`, `generateTwitterSmallTalk`, `generateHotIssueTweet` 등 모든 AI 트윗 생성 함수가 `gemini-flash-latest`부터 `gemini-3.6-flash`까지 **최신 9종 모델 풀**을 완벽하게 공유하도록 일원화.
  - 특정 모델 Quota 소진/지연 시 자동으로 9개 모델을 순차 순회하여 무중단 AI 생성 보장.

### v3.9.3 (2026-08-20) - 🧠 카테고리/키워드 인식형 지능형 핫이슈 문맥 맞춤 엔진 탑재 & 동일 문구 반복 완전 해결
- 🎯 **카테고리/키워드 인식 엔진 (`generateSmartHeuristicIssueTweet`)**:
  - 연예/가십/금수저/태세전환, 사건/사고/사회, 경제/주식/코인, 테크/AI, 정책/정부 등 기사 본문과 제목의 핵심 키워드를 정밀 분석.
  - 기사 주제에 100% 꼭 들어맞는 30개 이상의 다채로운 1번(질문형) 및 2번(공감형) 템플릿을 동적으로 조합하여, API 키 미설정/장애 시에도 엉뚱한 문구가 절대 나가지 않도록 완벽 보장.
- 🚫 **고정 Fallback 문구('현실에서 어떻게 풀어나갈지가 관건...') 영구 삭제**:
  - 기사 맥락과 맞지 않던 단일 고정 템플릿을 완전히 제거하고 문맥 맞춤형 동적 생성기로 대체.

### v3.9.2 (2026-08-20) - 🌟 호감형 실시간 핫이슈 트윗 멘트 생성 엔진 탑재 (1번 질문형 & 2번 위트 공감형 자동 판단)
- 💡 **호감형 2-Style 자동 판단 엔진 (`summarizer.js - generateHotIssueTweet`)**:
  - **스타일 1 (현실 관전평 & 밸런스 질문형)**: 사회/경제/정책/테크/토론 이슈에 맞추어 1줄 상황 맥락 + 현실적 솔직 코멘트 + 엑친 답글 유도 질문으로 작성.
  - **스타일 2 (위트 있는 현실 공감형)**: 가십/연예/직장/사건/생활 이슈에 맞추어 1줄 사건 맥락 + 일상에 빗댄 피식/공감 유머로 작성.
- 🛡️ **비하/욕설/선 넘는 혐오 표현 100% 영구 박멸**:
  - 저급한 욕설, 조롱, 맹목적 비난을 전면 차단하고 독자가 즉시 이해할 수 있는 1줄 맥락을 반드시 포함하도록 규격화.
- 📱 **텔레그램 브리핑 파이프라인 연동 (`services/telegramQueueService.js`)**:
  - 10분 주기 실시간 핫이슈 발송 시 `generateHotIssueTweet`을 우선 호출하도록 연동.

### v3.9.1 (2026-08-20) - 🎨 대시보드 설정 UI 10분 주기 실시간 핫이슈 & 엑친 스몰톡 명칭 100% 동기화
- 🎨 **설정 모달 UI 최신화 (`public/index.html`, `public/app.js`)**:
  - 기존의 '1시간 텔레그램 핵심 브리핑' 표기를 **'10분 주기 실시간 핫이슈 & 엑친 스몰톡 텔레그램 브리핑'**으로 완전 동기화.
  - 설명 문구 및 체크박스 라벨, 즉시 발송 테스트 버튼 문구를 10분 단건 실시간 발송 사양에 맞게 전면 업데이트.

### v3.9.0 (2026-08-20) - ⏱️ 10분 주기 실시간 핫이슈 & 엑친 스몰톡 텔레그램 자동 브리핑 전면 개편
- ⏱️ **10분 주기 단건 발송 스케줄러 (`*/10 * * * *`)**:
  - 기존 1시간 5건 일괄 브리핑에서 **10분마다 1건씩 실시간 핫이슈 또는 엑친 소통 멘트를 단건 발송**하는 구조로 전면 개편.
  - 🌙 **야간 정적 모드 엄격 준수**: 00:00 ~ 06:59 KST 새벽 시간대에는 취침 방해 방지를 위해 발송을 완벽히 차단.
- 🔥 **실시간 핫이슈 AI 트윗 멘트 생성**:
  - 가십, 연예, 사건/사고, 전쟁, 테크 등 실시간 핫이슈를 트위터리안 스타일(매운맛 사이다, 리얼 리액션, 1줄 훅)로 AI가 자동 정제하여 발송.
- 💬 **엑친 소통 스몰톡/뻘글 AI 자동 생성 엔진 (`generateTwitterSmallTalk`)**:
  - 신규 핫이슈가 없거나 중복될 경우, 엑친들에게 질문 던지기(점심/야식 메뉴, 밸런스 게임, 직장인 공감, 현타 뻘글)를 Gemini AI가 자동 창작하여 발송.
- ✂️ **사진 스트레스 제로 & X 원클릭 포스팅**:
  - 불필요한 이미지 캡처 생성을 걷어내고 텍스트 중심 메시지와 **`[🚀 𝕏에 바로 올리기 (글 자동입력) ↗]`** 링크를 탑재하여, 갤러리/사진첩 접근 없이 1초 만에 [게시] 가능.

### v3.8.44 (2026-08-20) - 📝 순정 [기사 캡처 (제목만)] 카드뉴스 고정 & 𝕏 인텐트 기사 링크 제외(순수 텍스트 전송)
- 📝 **순정 [기사 캡처 (제목만)] 고정 렌더링 (`services/cardNewsServerService.js`)**:
  - 사진 영역 없이 깔끔한 화이트 포털 뉴스 헤드라인 캡처 카드 규격으로 고정 렌더링.
- ✂️ **X 작성창 기사 링크 파라미터 제외 (`services/telegramQueueService.js`)**:
  - X 웹 인텐트 URL에서 `&url=...` 기사 링크를 완전히 제거하여, 순수 AI 요약 트윗 글만 채워지도록 수정.

### v3.8.43 (2026-08-20) - ✅ 텔레그램 𝕏 올림 완료 실시간 상태 전환 & 📸 100% [제목+사진] 카드뉴스 규격 일원화

- 🎯 **텔레그램 메시지 실시간 상태 마킹 (`services/telegramQueueService.js`)**:
  - 텔레그램에서 `[🚀 𝕏에 올리기]`를 누르면 즉시 메시지 상단에 **`✅ [𝕏에 올림 완료]`** 체크마크가 새겨져 이미 올린 기사인지 100% 구분 가능.
  - 버튼은 **`[🌐 𝕏 작성창으로 이동 ↗]`** 링크 버튼으로 변환되어 글이 채워진 작성창으로 즉시 이동.
- 🎨 **100% [제목+사진] 카드뉴스 렌더링 통일 (`services/cardNewsServerService.js`)**:
  - 실제 기사에 사진이 있으면 원본 고화질 사진을 탑재하고, 사진이 없는 기사라도 세련된 프리미엄 테크/뉴스 스페셜 리포트 그래픽을 자동 합성하여 **무조건 `📸 [제목+사진]` 규격**으로만 일원화 렌더링.

### v3.8.42 (2026-08-20) - 🖼️ X 웹 인텐트에 기사 원문 URL 자동 연동 & X 대형 이미지 프리뷰 카드 자동 노출

- 🔗 **기사 원문 링크 인텐트 파라미터 자동 삽입 (`services/telegramQueueService.js`)**:
  - `https://twitter.com/intent/tweet?text=...&url=...` 형태로 기사 링크를 자동 포함시켜, 트윗 발행 시 X 플랫폼 자체의 **고화질 기사 썸네일/사진 프리뷰 카드**가 하단에 자동으로 생성되도록 고도화.

### v3.8.41 (2026-08-20) - 🚀 100% 무료 평생 무제한 𝕏 원클릭 포스팅 전환 & 일일 갯수 제한 완전 제거

- 💎 **직통 1-Click X Web Intent 아키텍처 전환 (`services/telegramQueueService.js`)**:
  - 불안정하고 유료 결제를 유도하는 X API 대신, 텔레그램 메시지 발송 시 **`[🚀 𝕏에 바로 올리기 ↗]`** 직통 인텐트 링크 버튼을 기본 탑재.
  - 버튼을 누르면 스마트폰 X 앱 / PC 브라우저가 열리며 글이 100% 채워진 작성창으로 즉시 이동하여 1초 만에 [게시] 가능.
- ♾️ **일일 50개 갯수 제한 완전 제거**:
  - API 크레딧 제약이 완전히 사라졌으므로 일일 포스팅 갯수 제한을 무제한으로 해제.
- 🧹 **설정 UI 최적화 (`index.html`, `app.js`)**:
  - 불필요한 X API 키 입력 부담을 없애고 텔레그램 브리핑 전용 봇 설정만으로 깔끔하게 동작하도록 UI 정리.

### v3.8.40 (2026-08-20) - 🌐 X API 오류(credits depleted 등) 발생 시 즉시 원클릭 웹/앱 올리기 버튼 자동 전환

- 🛡️ **무중단 텔레그램 인텐트 링크 폴백 (`services/telegramQueueService.js`)**:
  - X API의 크레딧 소진(`credits depleted`) 또는 API 한도 에러 발생 시, 텔레그램 버튼을 **`[🌐 𝕏 웹/앱에서 직접 올리기 ↗]`** 인텐트 링크로 즉시 교체.
  - 사용자가 버튼을 누르면 트윗 내용이 채워진 X 앱/웹 작성창이 즉시 열려 1초 만에 무료로 직접 트윗을 발행할 수 있도록 지원.

### v3.8.39 (2026-08-20) - 🛡️ X 트윗 포스팅 글자수 안전 슬라이스 및 권한(Read and Write) 진단 에러 핸들러 강화

- 📝 **트윗 길이 자동 안전 규격화 (`services/xPostService.js`)**:
  - X API의 280 글자수 가중치 한도 초과로 인한 게시 실패를 방지하기 위해 245자 자동 안전 슬라이스 및 말줄임표 처리.
- 🔐 **X App 쓰기 권한(Read and Write) 안내 진단 로깅**:
  - 403 Forbidden 권한 에러 발생 시 X Developer Portal의 App Permissions 변경 및 Access Token 재발급 필요성을 명확히 진단 안내.

### v3.8.38 (2026-08-20) - 📸 기사 원문 실시간 이미지 능동 추출 엔진(`resolveArticleImageUrl`) 탑재 & [제목+사진] 카드뉴스 100% 보장

- 🔍 **지능형 실시간 이미지 크롤러 내장 (`services/cardNewsServerService.js`)**:
  - 기사 DB에 `imageUrl`이 비어 있더라도, 구글 뉴스 리다이렉트 원문 추적 및 네이버/네이트판/블라인드/더쿠/언론사 본문의 `og:image`, `twitter:image`, `<img>` 태그를 실시간 능동 크롤링하여 고화질 사진을 정확히 탐색.
  - 사진이 있는 기사는 누락 없이 100% **`📸 [기사 캡처 (제목+사진)]`** 모드로 완벽 렌더링.
  - 원문에도 사진이 전혀 없는 순수 텍스트 기사만 **`📝 [기사 캡처 (제목만)]`**으로 처리.

### v3.8.37 (2026-08-20) - 🔑 X API 및 텔레그램 브리핑 봇 키 저장 상태 UI 표시 보강

- 👁️ **설정 로드 시 저장 상태 시각화 (`app.js`)**:
  - 보안을 위해 비밀번호 필드에 원문 키를 노출하지 않으면서도, 이미 서버에 저장되어 있는 키는 `✅ [저장됨] ••••••••` 형태로 placeholder에 명확히 표기하여 사용자가 저장 여부를 즉시 식별 가능하도록 개선.

### v3.8.36 (2026-08-20) - 🤖 서버 접속 링크 봇 & 𝕏 기사 승인 브리핑 봇 완전 분리 지원

- 📲 **텔레그램 알림 채널/봇 이원화 (`config.js`, `services/telegramQueueService.js`, `index.html`)**:
  - 서버 시작 시 날아오는 '외부 접속 링크 알림'과 매시간 'X 기사 승인 브리핑'이 하나의 봇에 섞여 번잡해지는 것을 방지.
  - 설정 탭에 **`[🤖 기사 승인 브리핑 전용 봇 토큰 / Chat ID]`** 설정 필드를 신설하여, 완전히 별도의 봇/채팅방으로 기사 카드뉴스를 받아볼 수 있도록 독립 분리 지원. (미입력 시 기본 봇으로 단일 운용도 가능)

### v3.8.35 (2026-08-20) - 📸 텔레그램 카드뉴스를 대시보드 순정 [기사 캡처 (제목+사진/제목만)] 디자인으로 1:1 완벽 일원화

- 🎨 **대시보드 캔버스 디자인 100% 일원화 (`services/cardNewsServerService.js`)**:
  - 임의의 그래픽 디자인 대신, 사용자가 원래 쓰시던 대시보드의 **`[📸 기사 캡처 (제목+사진)]`** 및 **`[📝 기사 캡처 (제목만)]`** 순정 화이트 뉴스 포털 레이아웃과 폰트/구분선/뱃지를 그대로 서버 사이드 렌더러에 이식.
  - 기사에 사진이 있으면 ➡️ 순정 **`[제목+사진]`** 캡처 카드 생성.
  - 기사에 사진이 없으면 ➡️ 순정 **`[제목만]`** 캡처 카드 생성.

### v3.8.34 (2026-08-20) - 🪟 도스창 10초 후 자동 숨김(hide_window.ps1) Windows 10/11 전 환경 100% 호환성 강화

- 🛠️ **3중 다중 콘솔 핸들 감지 엔진 (`hide_window.ps1`)**:
  - `FindWindow` 창 제목 검색, `Get-Process` 메인 윈도우 핸들 일괄 탐색, `kernel32 GetConsoleWindow` 직통 호출의 3단계 복합 파이프라인 적용.
  - Windows Terminal, 기본 cmd, powershell 호스트 등 모든 OS 환경에서 10초 후 100% 깔끔하게 백그라운드로 자동 숨김 보장.

### v3.8.33 (2026-08-20) - 🛡️ sharp 모듈 미설치 환경 크래시 방어 핫픽스 & 배치 파일 자동 npm install 탑재

- 🛠️ **`sharp` 모듈 로드 예외 안전 방어막 (`services/cardNewsServerService.js`)**:
  - 서버 PC에 `sharp` 라이브러리가 아직 설치되지 않았더라도 서버 기동이 중단(Crash)되지 않도록 `try-catch` 안전 fallback 로직 적용.
- 📦 **배치 파일 자동 패키지 설치 파이프라인 (`실행하기.bat`, `코드_업데이트받기.bat`)**:
  - `git pull` 후 누락된 신규 패키지(`sharp` 등)가 자동으로 `npm install`되도록 배치 스크립트 고도화.

### v3.8.32 (2026-08-20) - ⏰ 1시간 주기 정각 핵심 5개 엄선 브리핑 & 🌙 00:00~07:00 야간 정적 모드 탑재

- ⏰ **1시간 정각 자동 브리핑 전환 (`services/telegramQueueService.js`, `scheduler.js`)**:
  - 알림 피로도를 최소화하기 위해 3분 스트림 대신 **매시간 정각 수집 직후 중복 없는 핵심 기사 5개**를 엄선하여 텔레그램으로 일괄 브리핑.
  - 카테고리 균형 알고리즘: IT/반도체, 코인/크립토, 주식/경제, 커뮤니티/썰/엔터를 골고루 5개 균형 배치.
- 🌙 **야간 취침 방해 방지 정적 모드 (00:00 ~ 07:00 KST)**:
  - 밤 12시(00:00)부터 아침 7시(06:59) 사이에는 텔레그램 알림을 자동 차단(정적 모드).
  - 아침 07:00부터 밤 23:00까지만 1시간 주기로 발송.
- ⚡ **수동 즉시 발송 버튼 고도화 (`index.html`, `server.js`)**:
  - 대시보드에서 `[⚡ 즉시 핵심 5개 텔레그램 발송]` 클릭 시 야간 시간대 무관하게 지금 즉시 5개 발송 테스트 가능.

### v3.8.31 (2026-08-20) - 🛡️ 텔레그램 X 포스팅 일일 50개 자동 카운터 & 한도 초과 시 𝕏 웹 인텐트 직접 올리기 자동 전환

- 📊 **일일 X 포스팅 카운터 추적기 (`history.js`)**:
  - KST 자정(00:00) 기준 매일 리셋되는 일일 포스팅 카운팅 엔진(`daily_x_post_counts.json`) 탑재.
  - 텔레그램에서 승인 포스팅 성공 시 `(오늘 X/50개)` 실시간 진행률 표기.
- 🌐 **50개 초과 시 자동 웹 링크 전환 폴백 (`services/telegramQueueService.js`)**:
  - 하루 50개 API 무료 할당량에 도달하면 X API 호출을 자동 차단하여 API 쿼터를 안전하게 보호.
  - 텔레그램 메시지 버튼을 **`[🌐 𝕏 웹에서 직접 올리기 ↗]`** 트위터 인텐트 링크로 즉시 교체하여 사용자가 웹 브라우저에서 무제한으로 직접 올릴 수 있도록 지원.

### v3.8.30 (2026-08-20) - 🤖 3분 텔레그램 승인 큐 발송 & 𝕏(트위터) 원클릭 자동 포스팅 파이프라인 탑재

- 📱 **3분 간격 텔레그램 승인 큐 워커 (`services/telegramQueueService.js`)**:
  - 미발행된 최신 기사를 3분마다 1건씩 AI 요약 트윗 및 고화질 카드뉴스와 함께 텔레그램 봇으로 전송.
  - 인라인 버튼 `[🚀 X에 올리기]` / `[❌ 건너뛰기]` 탑재로 사용자가 텔레그램에서 승인 시 즉시 X로 트윗 발행.
- 🎨 **사진 유무별 지능형 카드뉴스 렌더러 (`services/cardNewsServerService.js`)**:
  - 기사 원본에 사진이 있는 경우 ➡️ **`[제목 + 사진]`** 레이아웃 고화질 PNG 렌더링.
  - 기사 원본에 사진이 없는 경우 ➡️ **`[제목만]`** 다크모드 하이엔드 레이아웃 고화질 PNG 렌더링.
- 🚀 **X(트위터) API v1.1/v2 원터치 포스팅 엔진 (`services/xPostService.js`)**:
  - 이미지 미디어 업로드 및 트윗 텍스트 동시 발행 지원.
  - 포스팅 성공 시 트윗 URL 링크 회신 및 텔레그램 메시지 상태 자동 갱신.
- ⚙️ **대시보드 UI 연동 (`index.html`, `app.js`, `server.js`)**:
  - 설정 탭에 X API 키 4종 입력 및 계정 연결 확인, 3분 큐 ON/OFF 토글, 즉시 1건 텔레그램 발송 트리거 버튼 장착.
- 🧩 **완전 독립 모듈 구조**:
  - 추후 자동 포스팅 기능 중단 또는 삭제 요청 시 다른 스크래핑/대시보드 코드에 영향 없이 플래그 토글 또는 단일 모듈 제거 가능.

### v3.8.29 (2026-08-19) - 🌟 4. 하이브리드 후킹 모드 고정 멘트('결론부터 박고 시작함') 제거 및 다채로운 훅 어법 전면 개편

- 🎯 **하이브리드 후킹 도입부 다각화 (`summarizer.js`)**:
  - 매번 '결론부터 박고 시작함'으로 시작하던 앵무새 같은 고정 예시를 전면 금지(Negative Rule) 처리.
  - 기사 본질과 상황에 맞춰 '핵심만 짚어드림', '다들 A만 보는데 진짜는 B', '시장이 오판하고 있는 부분', '모두가 놓친 핵심 시그널' 등 7종 이상의 풍성하고 날카로운 도입 훅과 다채로운 마무리로 전면 개편.

### v3.8.28 (2026-08-19) - 💬 더쿠 핫게 픽 4종 타겟 쿼리 대량 수집(80건) & 실제 날짜 정밀 파싱 엔진 탑재
- 🚀 **더쿠 핫게 픽 수집량 대폭 확대 (`visualMediaService.js`)**:
  - 기존 단일 쿼리(1페이지, 5~10건) 한계를 극복하고 4종 다중 타겟 쿼리(`더쿠 핫게`, `스퀘어 짤/화보/직캠`, `움짤/비주얼`, `hot`) 및 다중 페이지 병렬 조회로 최대 80건까지 고화질 짤/움짤 대량 확보.
- 📅 **수집 날짜 강제 고정 버그 완벽 해결**:
  - 모든 더쿠 카드에 현재 시각(17:10)이 일괄 표기되던 하드코딩을 제거하고, 게시글 URL/본문/설명문에서 실제 등록 일자(`YYYY.MM.DD` / `YYYY.MM` / `YYYY`)를 정밀 추출하여 정상 표기.

### v3.8.27 (2026-08-19) - ⚡ 1. 이모지 요약 모드 프롬프트 심층 시장/산업 분석형으로 전면 고도화
- 📊 **1. 이모지 요약 모드 분석력 강화 (`summarizer.js`)**:
  - 기존 4종 무작위 스타일(호들갑 리액션, 어그로 등)로 인해 가벼운 잡담처럼 나오던 문제를 전면 개편.
  - **[1줄 본질 헤드라인 훅] + [📌 ⚡ 💡 3대 핵심 팩트 이모지 요약] + [전문가 관점 심층 분석 및 시사점 결론]** 구조로 개편하여 깊이 있는 분석 트윗 생성 보장.

### v3.8.22 (2026-08-19) - 📅 시각 매체 날짜/시간 시스템 완벽 개편 & 백업 터널 탑재 완료
- 🚨 **Cloudflare 터널 429 차단 시 Localtunnel 우회 엔진 탑재 (`server.js`)**:
  - Cloudflare Quick Tunnel이 너무 많은 요청으로 일시 차단(429)되었을 때 에러(EINVAL 등)를 캐치하여, 즉각적으로 `localtunnel`을 2차 비상망으로 가동하는 스마트 우회 파이프라인 구축 및 안정화.
- 🕒 **미디어 실시간 날짜 수집 보정 (`visualMediaService.js`)**:
  - 더쿠(Theqoo) 및 실시간 미디어 자료 수집 시 무지성으로 '최신' 키워드를 덮어쓰던 버그를 고치고, 수집한 바로 그 시점의 정확한 날짜와 시간(YYYY.MM.DD HH:mm)을 부여하도록 변경.
  - 외부 포털 자료도 날짜를 알 수 없는 경우 정직하게 '모름' 처리하여 트윗 AI가 혼동하지 않도록 유도.
- 🪟 **도스창 10초 후 자동 숨김 원상 복구 (`실행하기.bat`, `hide_window.ps1`)**:
  - 윈도우 환경에 구애받지 않고 무조건 창을 숨길 수 있도록 `hide_window.ps1`을 가장 안정적인 오리지널 버전으로 롤백.
- 📥 **`코드_업데이트받기.bat` 전용 배치 파일 신설**:
  - 서버를 켜지 않고 단순히 다른 작업 PC에서 최신 코드만 동기화할 수 있는 수동 업데이트 전용 툴 추가.

### v3.8.14 (2026-08-19) - 🛡️ 최후의 EADDRINUSE 방어선 (Node.js 레벨 자가 치유 로직) 탑재
- 🤖 **포트 충돌 경합(Race Condition) 완벽 타파 (`server.js`)**:
  - 배치 파일의 중복 실행이나 백그라운드 파워쉘 루프 등에 의해 `node.exe`가 0.1초 차이로 동시 다발적으로 띄워지더라도, 서버가 튕기지 않고 **1.5초 대기 후 최대 5회까지 포트를 재시도**하는 끈질긴 자가 치유(Self-Healing) 방어막 추가.
  - 최후에는 무조건 단 1개의 정상적인 노드 프로세스만 3000번 포트에 안착하도록 보장.

### v3.8.13 (2026-08-19) - 🛡️ EADDRINUSE (포트 충돌) 완벽 차단 및 안정화
- 🛠️ **`실행하기.bat` 루프 강제 킬(Kill) 100% 보장**:
  - 포트 3000을 점유하는 `node.exe` 잔여 프로세스를 `netstat` PID 방식뿐만 아니라 가장 원초적인 `taskkill /f /im node.exe` 방식으로 모든 무한 재시작 루프에 강제 삽입.
  - 이중, 삼중 킬(Kill) 안전장치로 완벽한 무중단 서버 자동 재기동 보장.
- 🎯 **더쿠(Theqoo) 최신 핫게 미디어 수집 엔진 최종 검증 완료 (`visualMediaService.js`, `index.html`)**:
  - 사용자 요구사항에 따라 `[💬 더쿠 핫게 픽]` 기능과 `fetchTheqooMedia` 함수가 소실되지 않고 정상 동작함을 최종 검증.

### v3.8.12 (2026-08-19) - 실행하기.bat 루프 내 3000번 포트 강제 종료 및 자동 git pull 탑재
- **[루프 내 포트 점유 해제 및 자동 복구]** (`실행하기.bat`):
  - `실행하기.bat`의 모든 실행 루프 진입점마다 3000번 포트를 잡고 있는 프로세스를 강제 taskkill하고 최신 코드를 git pull하도록 하여 EADDRINUSE 원천 차단

### v3.8.11 (2026-08-19) - server.js 블로킹 요소 전면 제거 및 어제 원본 순수 코드로 100% 완전 동기화
- **[서버 기동 블로킹 요소 완전 제거]** (`server.js`):
  - `app.listen()` 이전의 모든 `execSync` 프로세스를 완전 제거하고, 어제 100% 안정적으로 구동되던 순수 원본 `server.js`로 완전 동기화

### v3.8.10 (2026-08-19) - server.js의 라우트 닫는 괄호 SyntaxError 핫픽스
- **[구문 오류 완전 해결]** (`server.js`):
  - `app.post('/api/git-sync')`의 누락된 닫는 괄호 `});`를 복구하여 기동 오류 완전 해결

### v3.8.9 (2026-08-19) - app.listen 직전 포트 점유 유령 프로세스 OS 레벨 즉시 강제 킬(Kill) 안전장치 탑재
- **[포트 3000 점유 100% 원천 차단]** (`server.js`):
  - `app.listen()` 호출 직전에 PowerShell `Get-NetTCPConnection`을 통해 자기 자신(myPid)을 제외한 모든 점유 프로세스를 OS 레벨에서 강제 종료하여 EADDRINUSE 원천 방지

### v3.8.8 (2026-08-19) - startCloudflareTunnel 터널 생성 엔진을 어제의 검증된 100% 정상 작동 원본으로 완전 원복
- **[터널 기동 원본 복구 & 알림 메일 즉시 발송]** (`server.js`):
  - 비동기 exec 래퍼로 인해 터널 프로세스가 실행되지 않던 문제를 해결하고, 어제 100% 정상 작동하던 원본 `spawn(cloudflaredPath, ...)` 구조로 완전 복구

### v3.8.7 (2026-08-19) - 어제의 완벽했던 원본 기동 아키텍처로 100% 완전 원복 & 클린 안정화
- **[무한 재시도 루프 제거 및 원본 아키텍처 복구]** (`server.js`):
  - `server.on('error')`로 인한 불필요한 내부 재시도 루프를 완전히 제거하고, 어제 검증된 클린 `app.listen()` 구조로 원복

### v3.8.6 (2026-08-19) - Cloudflare 터널 생성 및 URL 추출 로직 비동기 안전화 & 정규식 완벽 호환
- **[터널 URL 감지 및 알림 발송 100% 보장]** (`server.js`):
  - 기존 프로세스를 비동기로 완전 종료 후 1초 뒤 안전하게 `cloudflared`를 실행하여 포트 충돌 방지
  - ANSI 및 대소문자/언더바/점 등 모든 `trycloudflare.com` 도메인 패턴을 완벽히 추출하도록 정규식 개선

### v3.8.5 (2026-08-19) - 포트 3000 EADDRINUSE 자기 치유(Self-Healing) 및 자동 복구 시스템 탑재

- **[포트 충돌 원천 방지 & 자기 치유]** (`server.js`):
  - `server.on('error')`에서 `EADDRINUSE` 감지 시 포트 3000을 점유하는 구버전 프로세스 및 터널을 강제 자동 정리 후 2초 만에 자체 재연결 수행

### v3.8.4 (2026-08-19) - visualMediaService.js의 export 식별자(fetchTheqooMedia) 불일치 긴급 수정

- **[ReferenceError 핫픽스]** (`visualMediaService.js`):
### v3.8.4 (2026-08-19) - visualMediaService.js의 export 식별자(fetchTheqooMedia) 불일치 긴급 수정

- **[ReferenceError 핫픽스]** (`visualMediaService.js`):
  - `module.exports`에 남아있던 구버전 미정의 식별자 `fetchCommunityMedia`를 `fetchTheqooMedia`로 정상 수정하여 런타임 크래시 완전 해결

### v3.8.3 (2026-08-19) - Cloudflare 터널 기동 시 기존 인스턴스 자동 정리 및 무중단 링크 전송 안정화

- **[터널 기동 안정화]** (`server.js`):
  - `startCloudflareTunnel()` 실행 전 남아있던 구버전 `cloudflared.exe` 프로세스를 자동 정리하여 충돌 방지
  - 서버 프로세스 종료 시 `process.on('exit')`로 터널 프로세스 안전 해제

### v3.8.2 (2026-08-19) - '💬 더쿠 핫게 픽' 전용 버튼 및 Cloudflare 우회 전용 파이프라인(fetchTheqooMedia) 추가

- **['💬 더쿠 핫게 픽' 전용 프리셋 버튼 신설]** (`public/index.html`, `visualMediaService.js`):
  - 직캠·화보 스튜디오 상단에 `💬 더쿠 핫게 픽` 전용 프리셋 버튼을 추가
- **[더쿠 Cloudflare 0% 차단 스마트 우회 엔진]** (`visualMediaService.js`):
  - Cloudflare 안티봇 방화벽을 우회하여 더쿠(theqoo.net) 핫게시판/스퀘어에 등록된 실시간 아이돌 짤/움짤을 100% 수집하는 `fetchTheqooMedia` 구현
  - 더쿠 핫게 버튼 클릭 시 더쿠 출처 짤들만 100% 모아서 반환

### v3.8.1 (2026-08-19) - 멀티 소스(Tenor, Naver, Bing, Daum) 동시 병행 융합(Blend) 수집 파이프라인 개방

- **[조기 반환(Early Return) 제거 및 전 소스 동시 개방]** (`visualMediaService.js`):
  - 기존 Tenor 수집 시 다음 소스가 차단되던 조기 반환 로직을 제거
  - `Tenor (MP4 직캠/움짤)` + `Naver HD (최신 국내 연예/포토)` + `Bing HD (글로벌 4K)` + `Daum HD`가 항상 골고루 함께 수집되도록 개방
  - 갤러리에 `💃 Tenor`, `🟢 Naver HD`, `🔍 Bing HD`, `💬 Daum HD` 등 다양한 출처가 풍성하게 융합되어 표시되도록 최적화

- **[YouTube API 의존성 완전 제거 & 스트레스 제로화]**:
  - 복잡하고 실효성 낮던 YouTube API 및 10,000 쿼터 제한 모니터링 시스템을 완전 제거
  - 쿼터 차감 걱정, API 키 발급/관리 스트레스 없이 100% 무제한 무료 미디어 탐색 시스템으로 전환
- **[최신 미디어 중심 4단 파이프라인 전면 고도화]** (`visualMediaService.js`):
  1. **Tenor Direct Video/GIF Archive**: 최신 K-POP 직캠 MP4 비디오 및 실시간 모션 움짤 우선 추출
  2. **더쿠(Theqoo) 연예 핫게시판**: 실시간 핫게시글 최신 아이돌/연예인 GIF & 사진 원본 파싱
  3. **DCInside 아이돌 갤러리**: 실시간 최신 갤러리 직캠/이미지 원본 수집
  4. **Bing & Naver HD Search**: 고화질 화보/사진 최신순 가중치 탐색
- **[UI/UX 최적화]**: 설정 탭의 불필요한 API 키 입력 카드를 정리하고, 직캠 갤러리 헤더를 `무제한 실시간 수집` 뱃지로 전환

### v3.7.5 (2026-08-19) - 전체 미디어 검색 5분 통합 캐시(visualMediaFullCache) 및 쿼터 동기화 기능 추가

- **[전체 미디어 5분 통합 메모리 캐시]** (`visualMediaService.js`):
  - `searchVisualMedia` 최상단에 전체 미디어 통합 5분 캐시를 적용하여, 버튼을 연타하거나 번갈아 클릭해도 5분 동안은 YouTube API 및 외부 크롤링을 100% 차단하고 즉시 캐시 데이터 반환 (쿼터 소모 0% 완벽 보장)
  - 결과가 0건이거나 실패했을 때도 캐시를 저장하여 반복적인 쿼터 낭비 원천 차단
- **[구글 콘솔 실시간 쿼터 동기화 기능]** (`quotaTracker.js`, `server.js`, `index.html`, `app.js`):
  - 설정 탭의 쿼터 모니터링 박스에 `[🔄 카운터 맞춤]` 버튼 추가 (`/api/reset-youtube-quota`)
  - 테스트 과정에서 올라간 대시보드 카운터를 실제 구글 콘솔 값(400 units / 4회)으로 즉시 맞춤/동기화 가능

### v3.7.4 (2026-08-19) - YouTube 쿼터 소진 및 차단 시 자동 제외 & 실시간 알림 시스템

- **[YouTube 쿼터 사전 감지 & 자동 제외]** (`visualMediaService.js`):
  - 일일 쿼터(10,000) 소진 시 불필요한 API 요청을 즉시 스킵하고, 커뮤니티(더쿠/DC/Tenor/Bing) 미디어로 매끄럽게 대체 수집
  - YouTube API 오류(키 무효/권한 미설정 등) 발생 시에도 사용자 화면 멈춤 없이 100% 정상 수집 지속
- **[사용자 명확 알림 시스템]** (`public/app.js`, `server.js`):
  - 쿼터 소진으로 YouTube가 제외될 시: `⚠️ YouTube 일일 쿼터 소진으로 YouTube가 제외되고, 커뮤니티(더쿠/DC/Tenor/Bing)로 대체 수집되었습니다.` 토스트 팝업 안내
  - API 키 오류로 제외될 시: `⚠️ YouTube API 키 오류로 YouTube가 제외되었습니다.` 토스트 팝업 안내

### v3.7.3 (2026-08-19) - YouTube 검색 5분 메모리 캐시(TTL) 시스템 도입 (중복 쿼터 낭비 방지)

- **[YouTube 스마트 5분 캐시 시스템]** (`visualMediaService.js`):
  - 동일 키워드(예: '여돌 움짤', '여돌 직캠 MP4') 검색 시 5분 동안 메모리 캐시를 재사용하여 쿼터 소모 0% 보장
  - 사용자가 프리셋 버튼을 연타하거나 반복 클릭해도 쿼터가 중복 차감되지 않고 0.01초 만에 즉시 로드
  - 새로운 키워드 검색 시에만 정상적으로 실시간 YouTube API 1회(100 units) 호출

### v3.7.2 (2026-08-19) - YouTube API 연결 테스트 쿼터 최적화 (1 unit 초경량 검증 전환)

- **[연결 테스트 쿼터 소모 0% 최적화]** (`visualMediaService.js`):
  - 기존 100 units가 소모되던 `search.list` 대신, 비용이 단 1 unit인 `videos.list` 초경량 검증 엔드포인트로 전환
  - 테스트 버튼 클릭 시 일일 직캠 검색 횟수 카운터 차감 제외 처리 (테스트를 수십 번 눌러도 하루 100회 직캠 검색 한도 보존)

### v3.7.1 (2026-08-19) - YouTube API 연결 검증 & 실시간 일일 쿼터(Quota) 트래커 및 출처 뱃지 시스템 도입

- **[YouTube API 상태 검증 & 연결 테스트]**:
  - 설정 탭에 `[⚡ 연결 테스트 & 정상 작동 확인]` 버튼 신설 (`/api/test-youtube` 엔드포인트)
  - API 키 유효성, 일일 할당량(Quota) 초과 여부, Google Cloud API 활성화 상태를 실시간 진단 및 한국어 피드백 제공
- **[일일 10,000 쿼터(Quota) 실시간 모니터링 시스템]** (`quotaTracker.js`):
  - KST 자정 기준 자동 리셋되는 YouTube Data API v3 일일 쿼터 추적 엔진 도입 (`data/youtube_quota.json`)
  - search.list 호출 시 1회당 100 units 차감 누적 기록
  - 설정 탭에 실시간 쿼터 프로그레스 바 (`오늘 사용량: X / 10,000 units (X%)`, `남은 검색 횟수: 약 Y회`) 및 Google Cloud 공식 콘솔 바로가기 링크 제공
  - 직캠 갤러리 탭 헤더에 콤팩트 실시간 쿼터 위젯 뱃지 추가
- **[미디어 출처(Source) 명확화 시스템]** (`public/app.js`):
  - 갤러리 카드에 직관적인 컬러 뱃지 시스템 도입:
    - 🎬 **YouTube**: `▶ YouTube` (레드 뱃지, 클릭 시 원본 유튜브 영상 새창 바로가기 링크 제공)
    - 💬 **더쿠**: `💬 더쿠 핫게` (보라색 뱃지)
    - 갤 **DCInside**: `갤 DC 갤러리` (블루 뱃지)
    - 💃 **Tenor**: `💃 Tenor GIF` (에메랄드 뱃지)
    - 🟢 **Naver / 🔍 Bing**: `Naver HD` / `Bing HD`
  - 우측 프리뷰 스튜디오 하단에 미디어 출처명 및 채널명, 원본 링크 바로가기(`[▶ 유튜브 원본 보기 ↗]`) 버튼 배치

### v3.7.0 (2026-08-19) - 대규모 리팩토링: 수집 다각화 & 프롬프트 고도화

- **[파트 1] 실 실시간 미디어 수집 다각화** (`visualMediaService.js`):
  - YouTube Data API v3 파이프라인 추가: 최근 24시간 이내 아이돌 직캠/영상 실시간 수집 (아이돌/영상 카테고리 한정)
  - 더쿠(Theqoo) 핫게시판 크롤링 시도 → 실패 시 DCInside 아이돌 갤러리 자동 폴백 파이프라인 구축
  - 기존 Tenor/Bing/Naver 파이프라인은 3~6번 단계로 유지 (하위 호환)
  - 직캠 태그 정밀화: `#여돌`, `#직캠` 등 포괄 태그 대신 `#카리나`, `#에스파`, `#장원영`, `#아이브` 등 구체적 아이돌명/그룹명 태그 + `YYMMDD_직캠` 날짜 태그 자동 생성
- **[파트 2] 설정 UI 확장** (`index.html`, `app.js`, `config.js`):
  - 설정 탭에 YouTube Data API v3 키 입력 카드 추가 (🎬)
  - 설정 탭에 X Bearer Token 입력 카드 추가 (🐦, 읽기 전용 검색 API용)
  - `config.js`에 `youtubeApiKey`, `xBearerToken` 필드 추가
  - `app.js`에 `saveYoutubeApiKey()`, `saveXBearerToken()` 함수 및 이벤트 리스너 추가
- **[파트 3] 프롬프트 고도화** (`summarizer.js`):
  - 공통 금지어 Negative Prompt Block 도입: "요약하자면", "결론적으로", "놀랍게도", "과연", "안녕하세요" 등 봇 말투 절대 금지
  - temperature 랜덤화: 고정 0.7 → 0.75~0.85 사이 랜덤 (다양성 강제)
  - 테크/금융 block 모드에 4종 랜덤 스타일 배열 도입: [A] 시니컬 팩트체크 / [B] 호들갑 리액션 / [C] 어그로 질문형 / [D] 차분한 분석형
- **[제외] 파트 4 Thread 자동 포스팅**: X API 무료 플랜 월 POST 1,500건 제한으로 인해 제외 (수동 타래 방식 유지)

### v3.6.1 (2026-08-19) - 🕒 대시보드 상단 실시간 오늘 날짜 및 현재 시각(라이브 시계) 위젯 탑재
- 📅 **실시간 날짜 & 요일 자동 표시 (`index.html`, `app.js`, `style.css`)**:
  - 대시보드 최상단 중앙에 `2026년 08월 19일 (수)` 형식으로 오늘 날짜와 요일을 항시 표시.
  - 요일별 직관적 컬러 구분 (일요일: 코랄 레드, 토요일: 스카이 블루, 평일: 민트/시안).
- ⏱️ **초 단위 실시간 디지털 시계 & KST/LIVE 인디케이터 (`app.js`, `style.css`)**:
  - 1초 단위로 부드럽게 흐르는 `HH:MM:SS AM/PM` 정밀 디지털 시계 및 `KST` 한국 표준시 태그 탑재.
  - 초록색 에메랄드 펄스 애니메이션(`LIVE`) 뱃지를 장착하여 실시간 데이터 동기화 상태를 시각적으로 전달.
- 📱 **모바일/태블릿 반응형 레이아웃 최적화 (`style.css`)**:
  - 모바일 및 좁은 화면에서도 타이틀, 라이브 시계 카드, 기능 버튼들이 깨짐 없이 정돈되어 출력되도록 그리드/플렉스 구조 보강.

### v3.6.0 (2026-08-19) - ⚡ Tenor 비디오 타임스탬프 기반 최신순 정렬 보정 & [💃 여돌 움짤] 명칭 간소화

- ⏱️ **비디오/움짤 내림차순 정렬 파이프라인 보정 (`visualMediaService.js`)**:
  - Tenor 비디오 파이프라인 반환 전 Unix timestamp(`item.created`)를 실시간 분석하여 최신(2026/2025/2024년) 비디오가 항상 1순위로 배치되도록 정렬 로직 강화.
- 🏷️ **버튼 명칭 직관적 변경 (`index.html`, `visualMediaService.js`)**:
  - `[💃 여돌 레전드 움짤]` ➡️ **`[💃 여돌 움짤]`** 로 깔끔하게 변경.

### v3.5.9 (2026-08-19) - 🧹 기본 프리셋 3종으로 초슬림화 & 최신순(내림차순) 정렬 엔진 적용
- 🧼 **프리셋 카테고리 3종 단일화 (`index.html`, `visualMediaService.js`)**:
  - 난잡하던 12개 버튼을 모두 정리하고 가장 핵심적인 3개 기본 프리셋(`[🎬 여돌 직캠 MP4]`, `[💃 여돌 레전드 움짤]`, `[✨ 모델/화보 고화질]`)만 유지.
- 📅 **최신순(내림차순) 우선 정렬 엔진 구현 (`visualMediaService.js`)**:
  - 수집된 모든 미디어를 업로드 날짜 기준으로 분석하여, **가장 최신 날짜(최신, 2026년, 2025년 등) 데이터가 맨 앞/위**에 오고 오래된 데이터일수록 뒤로 가도록 자동 정렬.

### v3.5.8 (2026-08-19) - 🎬 모든 직캠·음방·핫클립 카테고리 100% 순수 비디오(MP4) 출력 보장
- 🛠️ **비디오 카테고리 조건문 판정 버그 수정 (`visualMediaService.js`)**:
  - `[🌟 음방/현장 직캠]` 클릭 시 비디오 플래그가 누락되어 정적 10년 전 사진(유키스 등)이 나오던 라우팅 버그 수정.
  - `직캠`, `음방`, `현장`, `핫클립`, `M2`, `CHOOM`, `MP4` 등 모든 영상 관련 카테고리가 100% 순수 움직이는 비디오(MP4) 엔진으로 직행하도록 보장.

### v3.5.7 (2026-08-19) - 🛡️ Tenor 비디오 영문 쿼리 매핑 정밀화 & 밈/스티커 노이즈 완전 차단
- 🎯 **정밀 K-Pop 비디오 타겟팅 (`visualMediaService.js`)**:
  - 한국어 복합 검색어로 인해 해외 비디오 엔진에서 엉뚱한 해외 밈(Happy Birthday, 캐릭터 스티커, 일반인 영상 등)이 튀어나오던 검색어 오염 버그 수정.
  - `[💎 M2/스튜디오춤]`, `[🌟 음방/현장 직캠]`, `[🎬 직캠 MP4]` 클릭 시 100% 검증된 K-Pop 걸그룹 무대/직캠(`studio choom`, `kpop girl group fancam`, `kpop idol stage dance`)으로 정밀 매핑하여 실제 아이돌 비디오만 깨끗하게 로드되도록 개선.

### v3.5.6 (2026-08-19) - 🔗 X/인스타/유튜브 다이렉트 링크 자동 추출기 & 방송사 전문 채널 프리셋 장착
- ⚡ **SNS 다이렉트 URL 원클릭 미디어 추출기 (`visualMediaService.js`, `index.html`)**:
  - 검색창에 X(트위터) 트윗 링크, 유튜브/쇼츠 링크, 또는 웹 미디어 주소를 복사해서 붙여넣고 `[⚡ 탐색]`을 누르면, 최고화질 원본 비디오/이미지와 본문 내용을 즉시 추출하여 스튜디오로 로드 및 AI 트윗 문구 자동 완성.
- 💎 **공식 방송사/전문 큐레이터 카테고리 2종 신설 (`visualMediaService.js`, `index.html`)**:
  - `[💎 M2/스튜디오춤 4K 직캠]`: Mnet M2, STUDIO CHOOM 등 K-Pop 1위 전문 채널의 초고화질 직캠 수집.
  - `[🌟 음방/현장 직캠]`: KBS, SBS, MBC 등 지상파 음악방송 및 현장 출근길 공식 핫클립 수집.

### v3.5.5 (2026-08-19) - 🧼 특정 인물 한정 카테고리 제거 & 범용 K-Pop 직캠 카테고리 단일화
- 🎯 **카테고리 범용화 (`visualMediaService.js`, `index.html`)**:
  - 특정 인물(카리나/장원영)로 편중되었던 버튼을 깔끔하게 제거하고, 모든 K-Pop 여돌 및 글로벌 핫클립을 아우르는 `[🎬 직캠 MP4 영상]`, `[💃 여돌 레전드 움짤]` 범용 카테고리로 단일화.

### v3.5.4 (2026-08-19) - 🎬 [직캠 MP4 영상] 전용 카테고리 복원 & 순수 고화질 움직이는 비디오 파이프라인 정상화
- 📹 **순수 MP4 비디오 카테고리 복원 (`visualMediaService.js`, `index.html`)**:
  - `[🎬 직캠 MP4 영상]`: 실제 움직이는 비디오 플레이어 및 원클릭 MP4 파일 다운로드가 보장되는 전용 영상 카테고리 최우선 복원.
  - `[🔥 카리나/장원영 직캠 MP4]`: 멈춰있는 썸네일 대신 카리나, 장원영, 에스파, 아이브의 **실제 무대/직캠 움직이는 `.mp4` 비디오**만 즉시 수집.
  - 불필요한 중국 드라마 숏폼이나 노이즈 썸네일을 완전 제거하고, 순수 K-Pop 여돌 및 모델 모션 비디오만 로드되도록 쿼리 타겟팅 최적화.

### v3.5.3 (2026-08-19) - ✈️ 오늘자 공항 출국·출근길 & 실시간 4K 직캠 무제한 무료 수집 엔진 탑재
- 🚀 **실시간 현장 핫클립 파이프라인 개통 (`visualMediaService.js`, `index.html`)**:
  - `[✈️ 오늘자 공항/출근길]`: 오늘/최근 며칠 사이 실제로 일어난 아이돌 공항 출국, 입국, 출근길 현장 미디어 실시간 크롤링.
  - `[🔥 실시간 4K 직캠]`: 이번 주 업로드된 에스파, 아이브, 카리나, 장원영 등 대세 아이돌의 초고화질 4K 60fps 직캠 핫클립 즉시 수집.
  - `[📅 12시간 전]`, `[📅 1일 전]` 등 실시간 업로드 시점 뱃지 연동 및 AI 맞춤형 최신 트렌드 트윗 멘트 자동 생성.

### v3.5.2 (2026-08-19) - 🧹 직관적 브라우저 다운로드 UI 단일화 & 불필요한 선택창 정리
- 🧼 **헷갈리던 폴더 선택 다이얼로그 제거 (`index.html`, `app.js`)**:
  - 클릭 시 윈도우 기본 시작 위치(내 문서)로 열려 혼선을 주던 브라우저 폴더 선택창(`[📁]`)을 제거하고 `[💾 미디어 다운로드]` 단일 버튼으로 직관적 통합.
  - 클릭 한 번으로 사용자 PC의 브라우저 다운로드 창에 고화질 파일이 즉시 깔끔하게 수신됩니다.

### v3.5.1 (2026-08-19) - 📁 클라이언트 PC 전용 폴더(`X_Media_Downloads`) 자동 생성 및 직행 스트림 시스템
- 🛠️ **접속자 컴퓨터에 전용 폴더 자동 생성 & 원클릭 연동 (`app.js`, `index.html`)**:
  - `[📁]` 버튼 클릭 시 사용자의 원하는 드라이브나 바탕화면을 1회 지정하면, 내부에 **`X_Media_Downloads` 전용 폴더가 자동으로 생성(`create: true`)** 되어 연결.
  - 이후 `[💾 다운로드]`를 누를 때마다 묻지도 따지지도 않고 해당 `X_Media_Downloads` 폴더 안으로 파일이 자동 직행 저장됩니다.
  - 전용 폴더를 지정하지 않은 경우에도 브라우저 기본 다운로드 파이프라인으로 안전하게 저장됩니다.

### v3.5.0 (2026-08-19) - 💻 접속자 PC(클라이언트) 즉시 직행 다운로드 파이프라인 최적화
- 🎯 **브라우저 네이티브 클라이언트 다운로드 전면 적용 (`app.js`, `index.html`)**:
  - 대시보드를 다른 PC나 노트북에서 원격으로 접속해 사용할 때, 서버 컴퓨터에 저장되거나 탐색기가 서버에서 열리는 오동작을 방지.
  - `[💾 미디어 다운로드]` 클릭 시 현재 접속 중인 **사용자 본인의 컴퓨터(브라우저 다운로드 폴더/바탕화면)** 로 고화질 원본 파일이 다이렉트로 즉시 다운로드되도록 개편.

### v3.4.9 (2026-08-19) - 📂 바탕화면 저장 시 윈도우 탐색기 파일 자동 포커스 & 팝업 오픈
- 🚀 **저장 즉시 윈도우 탐색기 자동 실행 (`server.js`)**:
  - `[💾 바탕화면 저장]` 버튼 클릭 시 `C:\Users\k3ide\Desktop\X_Media_Downloads` 폴더에 파일이 저장됨과 동시에, 윈도우 탐색기(`explorer.exe /select`)가 자동으로 열려 다운로드된 파일을 파란색으로 선택(Focus)해 보여줍니다.
  - 사용자가 폴더를 일일이 찾아 헤맬 필요 없이 저장된 파일이 바로 모니터 화면에 나타납니다.

### v3.4.8 (2026-08-19) - 👤 미디어 제목 내 실명(연예인/모델) 자동 인식 및 멘트 반영 & 어휘 다양성 강화
- 🌟 **실제 인물명 멘트 자연스러운 반영 (`visualMediaService.js`)**:
  - 미디어 제목에 연예인/모델/인플루언서 이름(예: 바바라 팔빈, 시드니 스위니, 카리나, 장원영, 안유진, 정호연, 윈터 등)이 있을 경우, AI가 트윗 멘트에 해당 인물명을 자연스럽게 녹여서 작성("바바라 팔빈 이 시절 미모는 진짜 전설이다..", "시드니 스위니 분위기 미쳤네", "카리나 착장 소화력 실화냐 ㄷㄷ").
- 🗣️ **반복 어휘 탈피 & 표현 다양화**:
  - '미쳤다', '레전드' 일변도에서 벗어나 '독보적', '갓벽', '탈인간급', '아우라', '홀렸다', '눈부시다', '존재감', '반칙', '취향저격' 등 다채로운 어휘 풀을 적극 가동.

### v3.4.7 (2026-08-19) - 💖 SNS 청순/존예 해외 인플루언서·스타 전용 비주얼 아카이브 파이프라인 개편
- 🌟 **고지식한 서양 이미지 전면 배제 & 트렌디한 SNS 청순 여신 라인업 구성 (`visualMediaService.js`, `index.html`)**:
  - 딱딱하고 부자연스러운 서양 이미지 대신 SNS에서 가장 핫하고 청순하며 예쁜 해외 스타 및 인플루언서 위주로 타겟팅 개편:
    1. `[💖 해외 SNS 여신/인플루언서]`: 바바라 팔빈, 인스타 핫걸 등 청순/미모 중심의 생동감 넘치는 비주얼 GIF & 사진 수집.
    2. `[✨ 청순 해외스타/셀럽]`: 시드니 스위니 등 SNS에서 화제성 높은 영(Young) 스타들의 귀엽고 청순한 모션 클립 수집.
    3. `[🎬 해외 핫클립 MP4]`: 틱톡/릴스 감성의 트렌디한 에스테틱 셀피 및 댄스 MP4 영상 다이렉트 수집.
  - Tenor Global 고화질 모션 파이프라인을 전면 연동하여 100% 엑박 없는 생생한 비주얼 보장.

### v3.4.6 (2026-08-19) - 🎯 글로벌/서양 모델 검색 엔진 영어 쿼리 매핑 및 국내 포털 오염 격리
- 🌐 **영미권 고화질 쿼리 변환 & 검색 엔진 언어 격리 (`visualMediaService.js`)**:
  - 한국어 검색어("서양 모델 화보")로 검색 시 국내 포털/국내 블로그의 한국인 연예인 글이 섞여 나오던 문제를 해결하기 위해, 글로벌 카테고리 요청 시 영미권 정밀 타겟 쿼리(`caucasian fashion model photoshoot`, `hollywood actress red carpet glamour` 등) 및 `en-US` 헤더로 자동 변환.
  - 글로벌 카테고리 탐색 시 국내 포털(네이버/다음) 수집을 원천 차단하여 순수 100% 외국인 모델/셀럽 고화질 미디어만 정확하게 로드되도록 최적화.

### v3.4.5 (2026-08-19) - 🌍 외국인/글로벌 모델·해외 셀럽·해외 핫클립 전용 카테고리 3종 신설
- 🌟 **글로벌 비주얼 프리셋 3종 추가 (`visualMediaService.js`, `index.html`)**:
  - `[🌍 서양/외국 모델]`: 글로벌 톱모델 및 인스타그램 해외 인플루언서 고화질 화보 수집.
  - `[👱‍♀️ 할리우드/해외셀럽]`: 할리우드 여배우, 레드카펫, 글로벌 셀럽 비주얼 수집.
  - `[🎬 해외 핫클립 MP4]`: 틱톡/해외 댄스 및 모션 바이럴 MP4 동영상 다이렉트 수집.
- 🏷️ 외국인 미디어에 최적화된 글로벌 해시태그(`#외국모델`, `#photoshoot`, `#할리우드`, `#해외셀럽`) 자동 추천 연동.

### v3.4.4 (2026-08-19) - 💾 바탕화면 전용 다운로드 폴더(`X_Media_Downloads`) 자동 저장 & 탐색기 연동
- 📂 **바탕화면 전용 폴더 자동 생성 및 직행 저장 파이프라인 (`server.js`, `app.js`, `index.html`)**:
  - `[💾 바탕화면 저장]` 버튼 클릭 시, 브라우저 기본 다운로드 폴더 대신 **사용자 바탕화면의 `X_Media_Downloads` 전용 폴더**로 실시간 고화질 다운로드 및 자동 저장.
  - 전용 폴더가 없으면 서버가 바탕화면에 자동으로 폴더를 생성.
  - 저장 버튼 바로 옆에 **`[📂]` 원클릭 폴더 열기 버튼**을 추가하여 윈도우 탐색기로 해당 바탕화면 폴더를 즉시 확인 가능.

### v3.4.3 (2026-08-19) - 🎭 '스크롤 내리다' 상투적 시작 클리셰 완전 금지 & 다채로운 문장 변주 엔진
- 🚫 **고정된 첫 문장 패턴 원천 금지 (`visualMediaService.js`)**:
  - 매번 "스크롤 내리다 멈칫함 ㄷㄷ", "스크롤 내리다가" 등으로 시작하던 틀에 박힌 AI 클리셰를 100% 금지.
  - "와 이건 분위기가 다 했다..", "사람이 어떻게 이렇게 생김? ㅋㅋㅋ", "이 컨셉 소화력 실화냐", "첫 컷부터 시선 뺏김", "실물 포스 살벌하네" 등 감탄사, 질문, 직관적 묘사 등 첫 문장 구조를 매번 신선하고 다채롭게 생성하도록 프롬프트 및 예시 템플릿 전면 개편.
  - Temperature(창의성)를 1.0으로 최상위 세팅하여 반복성 제로화.

### v3.4.2 (2026-08-19) - 🏷️ 본문 해시태그 강제 삽입 제거 & 원클릭 추천 해시태그 칩(Chip) 버튼 탑재
- 🧼 **본문 내 자동 해시태그 박멸 (`visualMediaService.js`)**:
  - AI 생성 멘트 본문 끝에 `#여돌 #레전드` 같은 태그가 강제로 붙지 않도록 원천 분리.
- 🎯 **원클릭 추천 해시태그 칩 UI 추가 (`index.html`, `app.js`, `server.js`)**:
  - 트윗 본문 하단에 AI가 분석한 맞춤형 추천 해시태그(예: `[+ #여돌]`, `[+ #직캠]`, `[+ #2023년]`, `[+ #레전드]`) 버튼들을 나열.
  - 사용자가 마음에 드는 태그 버튼을 클릭하면 본문 끝에 즉시 자동 추가되며, `[태그 지우기]`로 일괄 초기화도 가능.

### v3.4.1 (2026-08-19) - 🧠 타임 인텔리전스 AI: 미디어 연도 자동 인식 및 리얼 휴먼 시점 멘트 생성 엔진
- ⏳ **현재 시점(2026) vs 미디어 연도(예: 2023) 자동 비교 분석 (`visualMediaService.js`, `server.js`, `app.js`)**:
  - 과거 자료(2022~2024년 등)인데 "이번", "요즘 나온", "방금 올라온" 처럼 사기 치던 AI의 멍청한 시점 오류를 원천 박멸.
  - 과거 미디어일 경우 인간의 자연스러운 감탄 톤("다시 봐도 이때 폼 레전드", "언제 봐도 미쳤다", "2023년 이때 분위기 독보적이었는데", "주기적으로 봐줘야 하는 레전드 짤")으로 지능형 분기 생성.
  - 최신 미디어일 경우에만 최신 트렌드/요즘 폼 멘트 적용.

### v3.4.0 (2026-08-19) - 🚀 5단계 딥 패턴 날짜 파서(Deep Date Parsing Engine) 탑재
- 🔬 **연도 추출 성공률 대폭 극대화 (`visualMediaService.js`)**:
  - 기존의 단일 URL 경로 매칭 방식에서 벗어나 5단계 정밀 패턴 매칭 탑재:
    1. `YYYY/MM`, `YYYY-MM`, `YYYY_MM` 연월 파싱
    2. `YYYYMMDD` 날짜 포맷 파싱
    3. `YYYY년 M월` 한글 텍스트 파싱
    4. 원본 페이지 링크(`purl`) 및 게시글 설명글(`desc`, `title`) 종합 스캔
    5. 단독 `YYYY` 연도 캡처
  - 연도 미상 비율을 획기적으로 낮추고 실제 업로드 연도를 대부분 찾아내어 뱃지에 표시하도록 엔진 고도화.

### v3.3.9 (2026-08-19) - 🏷️ 날짜 미상 미디어 뱃지 '연도미상' 명확화 처리
- 🏷️ **날짜 뱃지 텍스트 정제 (`visualMediaService.js`, `app.js`)**:
  - URL 경로에서 연월을 특정할 수 없는 미디어의 뱃지 문구를 애매한 '최신' 대신 **`[📅 연도미상]`**으로 명확하게 변경하여 혼선 방지.

### v3.3.8 (2026-08-19) - 📅 미디어 URL 및 메타데이터 기반 실제 업로드 연월(예: 2024.05) 정밀 추출 엔진 적용
- 🔍 **정밀 날짜 파싱 엔진 탑재 (`visualMediaService.js`)**:
  - 기존에 '최신'으로만 뭉뚱그려 표시되던 부분을 개선하여, 이미지/게시글 원본 URL 경로 및 메타데이터에서 실제 업로드 연월(예: `2024.05`, `2023.11` 등)을 정규식으로 파싱하여 날짜 뱃지에 정확하게 표시.

### v3.3.7 (2026-08-19) - 🏷️ 미디어 카드 및 스튜디오 미리보기 날짜 뱃지 상시 표시 업그레이드
- 💎 **날짜 뱃지 시각화 강화 (`app.js`)**:
  - 미디어 카드 좌측 상단 및 우측 스튜디오 미리보기 화면에 눈에 띄는 스카이블루 `[📅 2026.08]` / `[📅 최신]` 날짜 뱃지를 상시 표시하도록 개선.

### v3.3.6 (2026-08-18) - 🛡️ 오염된 검색 필터 제거 및 본래 고화질 여돌/모델 직캠 검색 엔진 원복 핫픽스
- 🔄 **검색 엔진 오염 유발 필터 원천 롤백 (`visualMediaService.js`)**:
  - Bing 검색 필터(`filterui:age-lt10080`) 추가로 인해 검색 알고리즘이 엉뚱한 헬스/남성 짤 및 축구 영상 등 오염된 결과를 긁어오던 문제를 발견하고 해당 필터를 즉시 제거 및 본래의 완벽했던 고화질 여돌/모델 화보 파이프라인으로 100% 원복.

### v3.3.5 (2026-08-18) - 📅 미디어 등록 날짜 뱃지 표기 및 최신 미디어 우선 수집 필터 탑재
- 🏷️ **미디어 카드별 날짜 뱃지 표기 (`app.js`)**:
  - 각 미디어 카드 상단에 촬영/등록 일자(예: `📅 2026.08` 또는 `📅 최신`) 뱃지를 깔끔하게 표시하여 오래된 미디어인지 한눈에 식별 가능하도록 개선.
- ⚡ **최신 미디어 우선 필터링 (`visualMediaService.js`)**:
  - 검색 엔진에 최신 주간/월간 신규 미디어 우선 필터(`qft=+filterui:age-lt10080`)를 적용하여 너무 오래된 과거 짤 대신 최신 트렌드 위주로 수집되도록 최적화.

### v3.3.4 (2026-08-18) - 🚫 '탐라' 등 거슬리는 폐쇄적 은어 전면 제거 및 보편적 리얼 구어체 적용
- 🧹 **낯설고 어색한 슬랭 배제 & 가독성 높은 피드 구어체 정제 (`visualMediaService.js`)**:
  - 일반 대중에게 생소하고 기계적으로 남발되던 '탐라(타임라인)' 단어를 100% 금지어 처리.
  - "피드", "스크롤 내리다가", "알고리즘", "인터넷 하다가" 등 직관적이고 공감대 높은 보편적 구어체로 전면 교체.

### v3.3.3 (2026-08-18) - 💬 AI 느낌 완전 박멸: 리얼 트위터/커뮤니티 네이티브 구어체 엔진 업그레이드
- ✍️ **실제 폰으로 쓴 듯한 찰진 구어체 & 드립 프롬프트 전면 개편 (`visualMediaService.js`)**:
  - 딱딱하고 어색했던 안내원 말투("~골라주라", "~추천합니다")를 100% 영구 금지.
  - 2030 X(트위터) 및 엔터 커뮤니티 헤비 유저들이 실제 사용하는 리얼 구어체("~인 듯", "ㄷㄷ", "ㅋㅋㅋ", "실화냐", "폼 미쳤네", "살벌하다", "이러면 반칙이지")로 프롬프트와 예시 전면 업그레이드.
  - Temperature(창의성)를 0.95로 상향하여 항상 신선하고 다채로운 1~2줄 감탄/반응 멘트 생성.

### v3.3.2 (2026-08-18) - 🎬 이미지/움짤 6종 카테고리 분리 및 Tenor 직캠 MP4 순수 비디오 전용 카테고리 신설
- 📂 **카테고리 완벽 분리 & Tenor Direct MP4 엔진 탑재 (`visualMediaService.js`, `index.html`)**:
  - 기존 6개 카테고리(코스프레, 여돌 직캠, 모델, 수영복, 레이싱모델, 그라비아)는 **고화질 사진과 정품 직캠 움짤(GIF)**만 안정적으로 나오도록 유지.
  - 전용 **`[🎬 직캠 MP4 영상]`** 카테고리를 신규 추가하여, Tenor 글로벌 비디오 아카이브의 **순수 `.mp4` 동영상 파일**을 다이렉트로 실시간 수집 및 플레이어 재생/원클릭 MP4 다운로드 연동.

### v3.3.1 (2026-08-18) - 🛡️ 직접 다운로드 가능한 고화질 사진 및 정품 직캠 움짤(GIF) 전용 엔진 최적화
- 🎯 **유튜브 웹 링크 배제 및 100% 직접 저장 가능한 고화질 미디어 파이프라인 정제 (`visualMediaService.js`, `app.js`, `index.html`)**:
  - 검색 엔진의 유튜브 링크(다운로드 불가 웹페이지)를 원천 배제하고, X(트위터)에 즉시 업로드 가능한 **실제 고화질 사진 원본(JPG/PNG/WEBP) 및 모션 직캠 움짤(GIF)**만을 정밀 수집하도록 롤백 최적화.
  - `[💾 미디어 다운로드]` 시 실제 파일 바이너리 MimeType을 정밀 감지하여 `.gif`, `.png`, `.jpg` 확장자로 안전하게 자동 저장되도록 다운로더 엔진 개선.

### v3.3.0 (2026-08-18) - 🎬 실시간 웹 MP4 동영상·직캠 비디오 클립 탐색 및 플레이어 엔진 신설
- 🎥 **고화질 MP4 비디오 탐색 & 인스튜디오 플레이어 탑재 (`visualMediaService.js`, `index.html`, `app.js`)**:
  - 기존 사진/움짤 외에 **실제 웹 직캠 비디오, 쇼츠, 댄스 핫클립 동영상**까지 함께 실시간 탐색하는 4단계 수집 엔진 탑재.
  - 프리셋에 **`[🎬 핫클립/영상]`** 카테고리 신설 및 미디어 카드에 동영상 재생 시간(`03:29`) 및 `[🎬 동영상]` 전용 뱃지 표시.
  - 우측 스튜디오에 **실시간 비디오 플레이어(`<video>`)**가 연동되어 영상 자동 재생 및 미리보기 지원.
  - `[💾 미디어 다운로드]` 원클릭 시 고화질 비디오를 즉시 다운로드하여 X(트위터)에 원본 영상 파일 그대로 업로드 가능.

### v3.2.1 (2026-08-18) - 🛡️ 인물 실명 추측 전면 배제 및 비주얼·컨셉 중심 클린 바이럴 엔진 전환
- 🎯 **연예인/인물 이름 날조 위험 원천 제거 (`visualMediaService.js`)**:
  - Vision AI의 얼굴 인식 오작동(다른 인물 이름을 붙이는 문제)을 원천 차단하기 위해 인물 이름 추측 기능을 완전히 배제.
  - 사진의 실제 **비주얼, 피지컬, 의상 핏, 헤어/메이크업, 전체적인 무드와 컨셉**에 집중하여 트위터 트렌드에 최적화된 리얼 감탄 바이럴 트윗을 생성하도록 프롬프트 전면 정제.

### v3.2.0 (2026-08-18) - 👁️ Gemini Vision 멀티모달(시각 이미지 분석) AI 인물 식별 엔진 탑재
- 🚀 **100% 무료 Gemini Vision 시각 데이터 다이렉트 분석 파이프라인 구축 (`visualMediaService.js`, `server.js`, `app.js`)**:
  - 사진/직캠 클릭 시 단순 텍스트가 아닌 **실제 고화질 이미지 원본 데이터(Base64)를 Gemini 2.5 Flash Vision 모델에 전송**.
  - AI가 사진 속 인물의 얼굴, 이목구비, 헤어스타일, 의상을 시각적으로 직접 확인하여 유명 연예인/아이돌/인플루언서인 경우 실명을 정확히 식별하여 트윗 본문 및 해시태그에 반영.
  - 일반 모델이나 특정할 수 없는 인물인 경우 엉뚱한 연예인 이름을 날조하지 않고 분위기/피지컬/의상 핏에 맞춤형으로 감탄하는 안전 시각 분석 지침 적용.
  - **비용**: 구글 무료 Gemini API 티어(`gemini-2.5-flash`)의 멀티모달 Vision 기능을 활용하여 **100% 무료(0원)**로 작동.

### v3.1.7 (2026-08-18) - 🛑 AI 연예인 이름 날조/할루시네이션 원천 차단 핫픽스
- 🔒 **인물 실명 날조 절대 금지 지침 강화 (`visualMediaService.js`)**:
  - 제목이나 검색어에 특정 인물의 이름이 명시되지 않은 일반 모델/코스프레 사진임에도 카리나, 장원영 등 엉뚱한 연예인 이름을 AI가 지어내던 치명적 할루시네이션 버그 수정.
  - 프롬프트에서 모든 인물 예시를 제거하고, 오직 **미디어 제목/정보에 실명이 100% 명확히 적혀 있는 경우에만 이름을 기재**하고, 그 외에는 분위기/컨셉/비주얼 묘사로만 작성하도록 엄격 제한.

### v3.1.6 (2026-08-18) - 🌟 비주얼 화보/직캠 유명인·연예인 실명 인식 및 바이럴 트윗 본문 자동 삽입
- 🏷️ **실제 미디어 메타데이터 추출 & Gemini 인물 실명 식별 프롬프트 강화 (`visualMediaService.js`)**:
  - 미디어 수집 엔진에서 이미지의 실제 원본 게시글 제목(아이돌 이름, 모델명, 인플루언서 이름 등)을 정밀 파싱하여 미디어 타이틀로 주입.
  - Gemini AI 바이럴 트윗 생성 엔진에 **인물/연예인/인플루언서 실명 감지 및 본문/해시태그 필수 표기 규칙**을 추가하여, 사진 속 주인공(예: 장원영, 카리나, 오해원, 신유빈 등)의 이름을 자연스럽게 콕 집어 바이럴 트윗을 완성하도록 업그레이드.

### v3.1.5 (2026-08-18) - 🖼️ 바이럴 X 포스팅 스튜디오 이미지 클립보드 자동 복사(Ctrl+V) 엔진 연동
- 📋 **미디어 자동 복사 및 원클릭 복사 툴바 신설 (`app.js`, `index.html`)**:
  - 바이럴 X 포스팅 스튜디오에서 `[🌐 X 작성창 열기 (100% 자동 채움)]` 클릭 시, 선택된 비주얼 화보/직캠 이미지를 클립보드(`ClipboardItem` PNG blob 변환)에 100% 자동 복사하도록 연동.
  - X 작성창이 새 탭으로 열리면 본문 텍스트는 자동 입력되고, 사용자는 `Ctrl+V` (붙여넣기)만 누르면 고화질 이미지가 트윗에 즉시 첨부됨.
  - 하단 액션 툴바에 수동 `[🖼️ 이미지 복사]` 원클릭 전용 버튼 추가.

### v3.1.4 (2026-08-18) - 🛡️ 네이버 403 차단 원천 방어 3단계 하이브리드 미디어 수집 엔진 탑재
- 🚀 **Bing HD + Naver HD + Daum HD 3단계 고화질 미디어 파이프라인 (`visualMediaService.js`)**:
  - 네이버 이미지 검색 시 일부 IP에서 간헐적으로 발생하던 `HTTP 403 Forbidden` 차단으로 인해 미디어가 0건으로 나오던 문제 완벽 해결.
  - **1단계**: IP 차단율 0% 및 원본 고화질 직통 추출이 가능한 **Bing Image HD 엔진**을 메인으로 우선 가동하여 30건 풀 수집.
  - **2단계**: User-Agent 및 Referer 헤더를 보강한 **Naver Image HD 보조 엔진** 릴레이.
  - **3단계**: 정밀 정규식 파싱이 적용된 **Daum Image 백업 엔진** 연동으로 100% 무중단 안정적 미디어 수집 보장.

### v3.1.3 (2026-08-18) - 📸 웹 직캠·화보 스튜디오 전용 단독 토글 스위치 분리
- 🎛️ **직캠·화보 스튜디오 단독 토글 제어 (`index.html`, `app.js`)**:
  - 기존 커뮤니티 카테고리 5종(엔터 전체, 여돌, 블라인드, 가십, 멘탈) 및 대시보드 통계 카드는 항상 정상 표시되도록 원상 복구.
  - 사이드바 토글 스위치는 오직 **`📸 웹 직캠·화보 X 포스팅` 메뉴 버튼과 해당 스튜디오 화면(`tab-visual-media`)만 단독으로 ON/OFF 제어**하도록 정밀 분리 (기본값: OFF).
  - 토글이 꺼진 상태에서는 직캠 스튜디오 화면이 100% 은닉되며, 토글을 켰을 때만 메뉴와 스튜디오가 나타남.

### v3.1.2 (2026-08-18) - 🍿 Jook Life (트렌드 & 엔터 / 직캠·화보) 기본 비활성화(Default OFF) 및 완전 숨김 토글 제어
- 🎛️ **Jook Life 완전 숨김 토글 및 기본 OFF 설정 (`index.html`, `app.js`)**:
  - 사이드바 `🍿 Jook Life (트렌드 & 엔터)` 옆 심플 체크박스 토글의 기본값을 **OFF (비활성화)**로 설정.
  - 토글 OFF 시: 사이드바 6개 메뉴(직캠/여돌/커뮤니티 등), 대시보드 커뮤니티 통계 카드 2종, 커뮤니티 미니 리스트 섹션, 기사 필터 알약 버튼, 웹 직캠·화보 스튜디오(`tab-visual-media`)를 100% 완전 은닉 처리.
  - 토글 OFF 상태에서 직캠 탭 진입 시 자동으로 메인 대시보드로 안전 리다이렉트.

### v3.1.1 (2026-08-18) - 🌸 웹 화보(그라비아/룩북) 카테고리 수집 차단 버그 핫픽스
- 📸 **성인 인증 키워드 필터 우회 (`visualMediaService.js`, `index.html`)**:
  - 네이버/다음 이미지 검색 엔진에서 '그라비아 룩북 화보' 검색 시 19세 미만 성인인증 차단으로 인해 검색 결과가 0건(미디어가 없다고 나옴)이 되던 치명적 버그 수정.
  - 내부 검색 쿼리를 '일본 모델 화보'로 우회하여 성인 필터링을 회피하고 100% 정상 수집되도록 조치.

### v3.1.0 (2026-08-18) - 깃허브(GitHub) 원격 개발 및 30초 실시간 자동 배포 엔진 구축
- 🚀 **원격 데스크톱 없이 집/노트북에서 편하게 작업하는 원격 CI/CD 연동 (`gitAutoSync.js`, `server.js`, `index.html`, `app.js`)**:
  - **Git 환경 구축**: 서버 PC에 독립 MinGit 환경 구성 및 영구 PATH 등록 완료.
  - **30초 백그라운드 자동 동기화 (`gitAutoSync.js`)**: 집 PC/노트북에서 코드를 수정하고 `git push`만 하면, 서버 PC가 30초마다 자동으로 변경사항을 감지하여 `git pull` 및 서버 재시작을 100% 자동 실행.
  - **대시보드 1클릭 수동 동기화 버튼**: 상단 헤더에 `[🔄 깃허브 동기화]` 버튼을 신설하여, 스마트폰/모바일 환경에서도 언제든지 원터치로 최신 코드를 즉시 반영 가능.

### v3.0.0 (2026-08-18) - 웹 직캠·화보 & 핫클립 탐색 및 AI 바이럴 X 포스팅 스튜디오 신설
- 📸 **웹 비주얼 미디어 탐색 및 실시간 수집 허브 (`visualMediaService.js`, `server.js`, `index.html`, `app.js`, `style.css`)**:
  - 기존 뉴스/커뮤니티 수집 체계 및 카테고리(`2h`, `5d`)는 100% 원형 보존하면서, 독립 탭 **`[📸 웹 직캠·화보 X 포스팅]`**을 사이드바에 추가.
  - **프리셋 카테고리 원터치 탐색**: 👙 코스프레/화보, 💃 여돌 직캠/댄스, ✨ 모델/인플루언서, 🌊 수영복/비키니, 🏎️ 레이싱모델/치어리더, 🌸 그라비아/룩북.
  - **키워드 및 웹링크 직접 입력 지원**: 검색창에 원하는 주제를 검색하거나, 외부 이미지/MP4/GIF 웹 링크를 직접 붙여넣어 즉시 불러오기 가능.
  - **AI 바이럴 트윗 카피라이터**: 선택된 미디어에 맞춰 4가지 어그로/바이럴 문체(🔥 심쿵 알고리즘 훅, 🌸 2030 남성 쿨한 찬사, 💬 댓글 54배 토론 유도, 🌶️ 도파민 매운맛 썰)를 매번 다르고 신선하게 자동 생성.
  - **원클릭 포스팅 & 파일 다운로드**: `💾 고화질 사진/영상 파일 다운로드` 및 `🌐 X 작성창 100% 자동 채움 열기` 완벽 연동.

### v2.9.2 (2026-08-16) - 기계식 AI 요약 기호(🔹🧠💡) 100% 제거 및 리얼 휴먼 트윗 산문체 전면 개편
- ✍️ **100% 사람 냄새 나는 트윗 정제 엔진 구축 (`summarizer.js`)**:
  - `🔹`, `🧠`, `💡`, `1️⃣`, `2️⃣`, `3️⃣`, `핵심:` 같은 기계적인 AI 요약봇 머리말 기호를 100% 전면 금지.
  - 사용자가 두서없이 입력한 생각/메모를 실제 사람이 호흡을 가다듬고 엔터(줄바꿈)를 섞어 쓴 것 같은 매끄럽고 깊이 있는 휴먼 트윗 문장으로 정제하도록 전면 개편.
  - Gemini 멀티 모델 폴백 루프를 적용하여 분당 호출량 제한(429) 시에도 1초 만에 안정적 응답 보장.

### v2.9.1 (2026-08-16) - 저화질 AI 이미지/영상 생성 기능 제거 및 클린 UI 복원
- 🧹 **AI 이미지/영상 생성 모듈 제거 (`index.html`, `app.js`, `server.js`)**:
  - 사용자 피드백(저화질로 인한 실사용 불가)을 반영하여 무료 AI 이미지 및 영상 생성 기능/버튼/API를 깔끔하게 제거.
  - 기존의 검증된 고화질 뉴스 캡처 카드(제목+사진, 제목만, 제목+본문) 및 원본 기사 사진, 헤드라인 카드 선택 기능으로 깔끔하게 원복.

### v2.9.0 (2026-08-16) - X 바이럴 카피라이팅 엔진 전면 개편 (조회수/북마크/댓글 폭발 설계)
- 🚀 **AI 프롬프트 & 문체 전면 쇄신 (`summarizer.js`)**:
  - 딱딱한 "뉴스 요약 봇" 말투를 100% 제거하고, X 추천 알고리즘(댓글 54배, 북마크 20배, 리트윗 20배)을 자극하는 실전 바이럴 문장 구조로 전면 개편.
  - **1번 이모지 팩트체크**: `도발적 1줄 훅 + 1️⃣ 겉보기 팩트 + 2️⃣ 뒤에서 벌어지는 현실 + 3️⃣ 향후 터질 결론 + 📌 북마크 유도`.
  - **2번 1인칭 아티클**: `스크롤 정지 1줄 훅 + 1인칭 심층 관점론 + 🧵 성지순례 확신 경고`.
  - **3번 리얼 리액션**: 진짜 트위터 헤비유저가 폰으로 방금 쓴 듯한 날것의 1~2줄 감탄/충격 멘트.
  - **4번 단호박 칼럼**: `결론부터 선언 + 맹점 팩폭 + 💬 댓글 토론 유도`.
  - **5번 여돌 칭찬**: 2030 남성 시점의 쿨하고 담백한 1줄 비주얼 찬사.
  - **6번 매운맛 팩폭**: 뼈 때리는 사이다 직구와 도파민 터지는 유쾌한 팩폭 썰.

### v2.8.3 (2026-08-16) - Gemini 429 할당량 초과 완전 방어 (

### v2.8.2 (2026-08-16) - Google Gemini Image & FLUX.1 하이브리드 무료 AI 이미지 엔진 탑재
- 🎨 **Gemini AI + FLUX.1 자동 연동 (`mediaGenerator.js`)**:
  - Google Gemini Image 모델을 1차로 시도하고, 구글 정책상 무료 할당량(Quota) 제한 시 비용이 일절 들지 않는 초고화질 FLUX.1/Turbo 엔진으로 자동 즉시 전환되어 100% 무료로 무제한 8K 이미지 생성 보장.

### v2.8.1 (2026-08-16) - 자유 미디어 프롬프트 직접 생성기 & 에디터 가로폭 와이드 확장
- 🎨 **자유 AI 포토/영상 프롬프트 직접 입력 바 신설 (`index.html`, `app.js`, `mediaGenerator.js`)**:
  - 기사 내용에 의존하지 않고, 사용자가 입력창에 적은 내용(예: "사이버펑크 서울 야경", "고윤정 스타일 화보")만으로 100% 독립 생성되도록 개선.
  - Turbo 고속 렌더링 폴백 및 타임아웃 예외 처리를 추가하여 비디오/이미지 생성 지연 문제 원천 해결.
- 📐 **트윗 생성 에디터 영역 와이드 확장 및 DOM 중복 태그 버그 수정 (`index.html`, `style.css`)**:
  - `index.html` 내 중복된 헤더 태그(`card-header`) 누락 닫기를 완벽 수정하여 에디터가 왼쪽 박스 안에 갇혀 세로로 찌그러지던 문제를 원천 해결.
  - `340px 1fr`의 쾌적하고 시원한 2컬럼 와이드 레이아웃으로 본문 텍스트 에디터 가로폭 대폭 확장.

### v2.8.0 (2026-08-16) - 무료 FLUX.1 AI 포토/영상 생성 & 내 생각/메모 기반 AI 트윗 변환기 탑재
- 🎨 **FLUX.1 무료 AI 포토 & 모션 비디오 생성 모듈 구축 (`mediaGenerator.js`, `server.js`)**:
  - API 키 없이 100% 무료로 사용할 수 있는 고화질 FLUX.1 이미지 및 모션 비디오 생성 엔진 연동.
  - 기사나 작성된 트윗 내용을 Gemini가 자동으로 8K 시네마틱 프롬프트로 변환하여 3~5초 만에 고화질 사진/비디오 클립을 생성 및 원클릭 다운로드 지원.
- 🎙️ **내 생각/자유 메모 ➔ AI 트윗 정제기 (My Thoughts Composer) 신설 (`index.html`, `app.js`, `summarizer.js`)**:
  - 기사 뉴스뿐만 아니라 사용자가 직접 음성 메모나 떠오르는 생각을 두서없이 적으면, 1초 만에 7가지 스타일(이모지 요약, 1인칭 아티클, 짤막 리액션, 융합 브릿지 등)의 고품질 X 트윗으로 정제하는 기능 탑재.

### v2.7.2 (2026-08-16) - 실사용 핵심 7종 스타일(짧은 리액션, 1인칭 아티클, 하이브리드 후킹 등) 확정 복원
- 🎯 **사용자 피드백 기반 스타일 재정렬 (`index.html`, `app.js`)**:
  - 비슷하게 느껴지던 불필요한 모드들을 걷어내고, 실제 활용도가 높은 7개 핵심 스타일(1. 이모지 요약, 2. 1인칭 아티클, 3. 짧은 리액션, 4. 하이브리드 후킹, 5. 여돌 칭찬, 6. 매운맛 꺼드럭, 7. 멘탈 공감)로 명확하고 직관적이게 재편성.

### v2.7.1 (2026-08-16) - 트윗 생성 스타일 핵심 5종 간소화 & 에디터 UI 클린 개편
- ✨ **스타일 5종 체계 통폐합 및 UI 최적화 (`index.html`, `app.js`)**:
  - 난잡했던 9개 스타일 버튼을 직관적인 핵심 5종(전문 분석, 융합 브릿지, 토론 질문, 여돌/비주얼, 썰/리액션)으로 깔끔하게 통폐합.
  - 코너 태그 및 질문 훅 툴바를 슬림형 상단 미니 바 형태로 통합하여 에디터 영역의 시각적 복잡도를 대폭 해소.

### v2.7.0 (2026-08-16) - 하이브리드 퍼스널 브랜딩(Insight + Life + Bridge 융합) 전면 개편
- 🏛️ **하이브리드 3대 축 전면 개편 (`index.html`, `app.js`, `summarizer.js`)**:
  - **📊 [Jook Insight]**: 테크/AI/반도체/금융 전문 분석 및 1인칭 아티클 코너.
  - **🍿 [Jook Life]**: 2030 남성 시점의 쿨한 여돌/비주얼 연예인, 커뮤니티, 팝컬처 코너.
  - **⚡ [Jook Bridge 융합 모드]**: 대중적 엔터/일상/이슈 훅 ➔ 본질적인 AI/반도체/시장 인사이트로 연결하는 최고 효율의 융합 포스팅 모드 신규 탑재.
- 🏷️ **원클릭 코너 브랜딩 & 인게이지먼트 부스터 툴바 신설 (`index.html`, `app.js`)**:
  - 에디터 상단에 `📊 #Jook_Insight`, `🍿 #Jook_Life`, `⚡ #Jook_Bridge` 코너 태그 원클릭 적용 툴바 추가.
  - 알고리즘 노출(For You 추천)을 극대화하기 위한 '원클릭 토론/질문 훅' (예: "다들 어떻게 생각하시나요? 👀", "어디까지 갈 거라고 보시나요? 💭") 버튼 툴바 탑재.

### v2.6.1 (2026-08-16) - 실제 성과 데이터 기반 프롬프트 어휘 다양화 & 템플릿 반복 방지
- 🎯 **실제 조회수 데이터 반영 및 버전 7 어휘 고도화 (`summarizer.js`)**:
  - '독보적', '인정할 수밖에 없음', '실화냐' 등 특정 단어 반복 패턴을 금지어로 지정하여 봇 같은 느낌 원천 차단.
  - 기사 속 구체적인 맥락(착장, 컨셉, 이목구비, 화보 분위기, 실물 포스 등)을 살려 매번 다른 남성 시점의 1줄 트윗이 생성되도록 프롬프트 및 폴백 전면 개편.

### v2.6.0 (2026-08-12) - 여돌/비주얼 연예인 포토 카테고리 & 버전 7 (여돌/비주얼 칭찬 모드) 추가
- 🌸 **여돌/비주얼 연예인 카테고리 수집 확장 (`scraper.js`)**:
  - 커뮤니티 카테고리에 '여자아이돌, 고윤정/장원영/카리나 등 비주얼 연예인, 화보 및 포토 기사' 수집 모듈(`🌸 아이돌 / 비주얼 연예인`) 신규 추가 (`when:5d` 5일 retention 규칙 준수).
- 💬 **버전 7: 여돌/비주얼 칭찬 & 자연스러운 부러움 모드 신규 도입 (`summarizer.js`, `app.js`, `index.html`)**:
  - 기사/포토 내용에 맞춰 "이쁜 애들은 진짜 이쁘다", "미모 미쳤다", "세상 혼자 사네" 같은 따뜻하고 긍정적인 칭찬 1문장 + 과하지 않고 솔직한 부러움 1문장(1~2줄 분량)의 전용 AI 프롬프트 구현.
  - 해당 기사 선택 시 자동으로 버전 7 모드가 선택되도록 스마트 인터페이스 연동.

### v2.5.3 (2026-08-10) - Cloudflare 터널 생성 시 텔레그램/이메일 알림 트리거 재연동
- 📲 **터널 접속 링크 자동 발송 트리거 보완 (`server.js`)**:
  - 파일 되돌리기(Revert) 과정에서 누락되었던 `notifyNewTunnelUrl(tunnelUrl)` 호출 구문을 `server.js`의 `startCloudflareTunnel()` 내부로 재연동.
  - PC 재부팅 또는 배치 파일 실행 시 새로 발급된 Cloudflare 외부 접속 URL이 텔레그램 및 이메일로 100% 자동 전송되도록 처리.

### v2.2.10 (2026-08-10) - 버전 6 (매운맛 꺼드럭 썰) 추가
- 📝 **사용자 피드백 기반 신규 프롬프트 모드 도입 (`summarizer.js`, `app.js`, `index.html`)**:
  - 도파민이 터지는 날것의 표현과 거침없는 말투를 구사하는 '버전 6 (매운맛 꺼드럭 썰)' 신규 추가.

### v2.2.9 (2026-08-09) - 프롬프트 모드 간소화 및 짧은 리액션 모드 추가
- 📝 **사용자 피드백 기반 모드 통폐합 (`summarizer.js`, `app.js`, `index.html`)**:
  - 기존의 에세이 성격이 강해 내용이 중복되던 버전 3, 5, 6을 삭제.
  - 신규 버전 3(짧은 리액션 모드) 추가: 1~2줄짜리 리얼하고 캐주얼한 커뮤니티형 리액션.
  - 전체 모드를 1~5번 체계로 재편성.

### v2.2.8 (2026-08-08) - X 오리지널 콘텐츠 리워드 프로그램 대응 전면 개편
- 📝 **프롬프트 생성 로직 개편 (`summarizer.js`)**:
  - X의 새로운 수익 창출 정책(참여 유도 금지, 타 플랫폼 재업로드 금지, 단순 요약 금지)에 맞춰 프롬프트 전면 수정.
  - 버전 5, 6의 캡처 유도 및 갈등 요약을 '직장/인간관계 심층 에세이'로 완전 전환.
  - 외부 링크 및 스레드(타래) 분리 로직 완전 삭제 (버전 7 등).
- ⚙️ **PROJECT_SPEC.md 갱신**: 프롬프트 모드 설명 수정 및 정책 사항 반영.

### v2.2.7 (2026-08-08) - 네이트판 기사 스크래핑 시 본문 상하단 메뉴 텍스트 혼입 버그 수정
- 🧹 **네이트판 불필요 텍스트 제거 강화 (`scraper.js`)**:
  - `fetchNatePannDetail` 함수에서 본문과 무관한 상단 내비게이션 바(네이트 판, 뉴스, 스포츠, 연예 등) 및 '이전글', '다음글', '추천수', '댓글달기' 등의 불필요한 텍스트 찌꺼기가 기사 본문에 딸려 수집되던 문제 해결.
  - 정규식을 이용해 `<nav>`, `<header>`, `<footer>`, `<aside>` 등의 태그 영역을 우선 제거하고, 텍스트 변환 후 이전글/다음글을 기준으로 본문만 정확히 잘라내도록 처리.

### v2.2.6 (2026-08-08) - Bing 백업 수집 시 OR 검색어 누락(0건 수집) 버그 수정
- 🔄 **Bing 쿼리 내 `OR` 연산자 보존 (`scraper.js`)**:
  - Bing News 백업 요청 시, 기존에는 구글 전용 쿼리를 변환하는 과정에서 `OR` 키워드를 공백으로 날려버리는 치명적인 버그가 존재했음.
  - 이로 인해 "비트코인 코인 이더리움" 처럼 모든 단어가 포함된(AND 조건) 극히 드문 기사만 검색되어, 최신 2시간 이내 기사가 0건으로 폐기되는 현상 발생.
  - 정규식 변환 로직에서 `OR` 키워드를 유지하여 Bing에서도 정상적인 다중 조건 검색이 가능하도록 수정.

### v2.2.5 (2026-08-08) - 구글 뉴스 RSS 연속 호출 차단(429) 방지
- 🛡️ **순차 수집 간격 증가 및 에러 로그 상세화 (`scraper.js`)**:
  - `fetchLatestArticles` 실행 시 12개가 넘는 카테고리를 0.12초(120ms) 간격으로 연속 호출하여 구글 서버가 봇(Bot)으로 감지하고 차단(429 Too Many Requests)하여 전면 Bing으로 넘어가던 문제 해결.
  - 각 카테고리 수집 간격을 **1.5초(1500ms)**로 늘려 구글 서버의 속도 제한(Rate Limit)을 안전하게 우회하도록 수정.
  - "구글 직통 지연" 로그 출력 시 실제 에러 메시지(예: `timeout of 4500ms exceeded` 또는 `Request failed with status code 429`)를 함께 출력하도록 디버깅 강화.

### v2.2.4 (2026-08-08) - Bing 백업 수집 시 과거 기사 노출 버그 수정
- 🔄 **Bing RSS Timeframe(시간 범위) 수동 필터링 적용 (`scraper.js`)**:
  - 구글 검색 실패 시 가동되는 2차 Bing RSS 백업 엔진이 구글 고유의 `when:2h` 쿼리를 해석하지 못해 1~2일 전의 오래된 기사를 가져오는 문제 해결.
  - Bing API URL에 `&qft=sortbydate="1"`(최신순 정렬) 옵션을 추가하고, 응답받은 기사 배열을 자바스크립트 Date 연산을 통해 사용자 설정 시간(`2h` 또는 `5d`) 초과 시 강제 필터링하여 드롭하도록 로직 개선.

### v2.2.3 (2026-08-08) - 대시보드 자동 새로고침(Auto-Refresh) 기능 추가
- 🔄 **수집 완료 시 화면 자동 갱신 (`server.js`, `public/app.js`)**:
  - `server.js`의 `/api/status` 응답에 `totalDbCount`를 포함하여 전체 기사 개수 변동을 추적.
  - 프론트엔드의 폴링 주기를 3초로 단축시켜, 백그라운드 수집(초기 부팅 수집 포함) 완료 시 DB 개수 증가를 감지하고 즉시 `window.location.reload()`를 실행하여 최신 상태를 자동 반영.
  - 수동 `[🔄 수집]` 버튼 클릭 시에도 1.5초 후 자동 새로고침 되도록 적용하여 사용자 경험 개선.

### v2.2.2 (2026-08-08) - 네이트판 타임존 파싱 버그 핫픽스
- ⚖️ **네이트판 미래 시간 표기 및 방금전 뭉뚱그림 현상 완벽 해결 (`scraper.js`)**:
  - `fetchNatePannDetail` 함수에서 추출된 한국 시간 문자열이 `Date` 객체로 변환될 때 서버 기준 타임존(UTC)으로 잘못 해석되어, 작성시간이 무려 **9시간 미래**로 뻥튀기되던 버그(UTC 시차 계산 오류) 해결.
  - 시간 문자열 뒤에 `+09:00` (KST 오프셋)을 명시적으로 붙여 파싱하도록 수정하여, 이제 글 작성 시간이 한국 시간으로 정확하게 반영됨.

### v2.2.1 (2026-08-08) - 네이트판 게시글 실제 작성시간 정밀 파싱 수정
- ⚖️ **네이트판 작성시간 초기화 버그 해결 (`scraper.js`)**:
  - `fetchNatePannArticles`에서 수집 시점 `new Date().toISOString()`으로 덮어써서 모든 네이트판 글이 `방금 전`으로 출력되던 원인 해결.
  - 게시글 본문 페이지 HTML에서 실제 작성일시(`YYYY.MM.DD HH:mm`)를 병렬로 정밀 추출하여 실제 작성시간(예: 8월 7일 23:52)을 100% 정상 복원.

### v2.2.0 (2026-08-08) - 1차 구글 뉴스 직통 수집 파이프라인 정립
- ⚡ **1차 구글 뉴스 직통 수집 배치 (`scraper.js`)**:
  - **1단계**: 구글 뉴스 직통 수집 (`⚡ [1차 구글 직통 성공]`, 타임아웃 4.5초)
  - **2단계**: Bing News RSS 백업 엔진 (`🔄 [2차 Bing 백업 성공]`, 5.0초)
  - **3단계**: rss2json 구글 릴레이 보조 백업 엔진 (`🌐 [3차 rss2json 구글 성공]`)
- 🧹 **Cloudflare Worker 전면 삭제 유지**: Cloudflare 관련 503 경고 코드 완전 제거.

### v2.1.0 (2026-08-08) - Cloudflare Worker 전면 삭제 및 수집 파이프라인 3단계 경량화 대개편
- 🧹 **Cloudflare Worker 우회로 완전히 삭제 (`scraper.js`, `server.js`)**:
  - 사용자 지침에 따라 불필요하고 503 오류를 유발하던 Cloudflare Worker 관련 상수 및 우회 로직을 전면 삭제.
- 🚀 **초고속 3단계 수집 파이프라인 정립 (`scraper.js`)**:
  - **1단계**: Bing News RSS 1차 초고속 메인 엔진 (0.2초 0% 차단 100% 정상 수집)
  - **2단계**: 구글 뉴스 직통 백업 엔진 (3.0초 타임아웃)
  - **3단계**: rss2json 구글 릴레이 보조 백업 엔진

### v2.0.0 (2026-08-08) - 1차 메인 수집 엔진 전격 개편 (0.2초 초고속 0% 차단 뉴스 수집 파이프라인)
- 🚀 **1차 메인 엔진 전환 (`scraper.js`)**:
  - 클라우드 IP 차단(503/429)이 전혀 없는 Bing News RSS 엔진을 1차 메인 수집 엔진(`🚀 [1차 메인 수집 성공]`)으로 전격 배치.
  - 전 13개 카테고리 총 105건 기사를 단 2초 만에 100% 정상 수집 완료.
  - 503 경고 및 타임아웃 지연 로그 100% 소멸.
- 🛡️ **보조 백업 수집 엔진 릴레이 (`scraper.js`)**:
  - 2차 구글 뉴스 직통 백업 및 3차 rss2json 릴레이로 수집 안정성 극대화.

### v1.8.1 (2026-08-08) - 1차 구글 뉴스 직접 수집 복원 (타임아웃 1.2s ➔ 4.5s 최적화)
- ⚡ **1차 구글 뉴스 직통 성공률 100% 복원 (`scraper.js`)**:
  - 기존 1차 구글 수집 타임아웃이 1.2초(`1200ms`)로 너무 짧아 클라우드 서버 응답 지연 시 곧바로 우회로 넘어가는 문제 해결.
  - 타임아웃을 4.5초(`4500ms`)로 여유 있게 확장하여 우회 통로(Cloudflare/Bing)를 거치지 않고 **`⚡ [1차 구글 직통 성공]`으로 직접 100% 수집 복원**!

### v1.8.0 (2026-08-08) - Cloudflare Worker 구글 뉴스 원본 100% 수집 복원 (Double Encoding 버그 완벽 해결)
- 🚀 **구글 원본 수집 503 에러 근본 원인 해결 (`scraper.js`, `server.js`)**:
  - `CF_WORKER_URL` 호출 시 이미 인코딩된 `googleUrl`을 또 한 번 중복 인코딩(`encodeURIComponent`)하여 구글이 인코딩 파라미터 미인식으로 503/타임아웃을 뿜어내던 치명적 버그 수정.
  - 단일 인코딩(Single Encoding)으로 정상화하여 Cloudflare Worker를 통한 **구글 뉴스 원본 수집(`🚀 [2차 Cloudflare 구글 성공]`) 100% 정상 수집 복원**!

### v1.7.8 (2026-08-08) - 서버 시작 시 초기 수집 자동 실행 복원 및 수집 중 F5 안전 보장
- 🚀 **서버 시작 시 초기 수집 자동 실행 복원 (`server.js`)**:
  - Render 서버 배포 및 재시작 2초 후 13개 카테고리 최신 뉴스를 1회 비동기로 자동 수집하도록 복원 (`🚀 [서버 시작] 최신 뉴스 초기 수집을 시작합니다...`).
- 🛡️ **수집 진행 중 F5 새로고침 안전 보장 (`public/app.js`, `server.js`)**:
  - 수집이 진행되는 도중 F5로 웹페이지를 새로고침하더라도 백그라운드 수집이 튕기거나 중복 수집이 폭주하지 않고 안전하게 완료.

### v1.7.7 (2026-08-08) - F5 웹페이지 새로고침 시 수집 트리거 오작동 완벽 차단
- 🚫 **F5 페이지 새로고침 수집 유발 버그 수정 (`server.js`)**:
  - 기존 서버 재시작 직후 메모리 캐시 초기화(`articlesCache.length === 0`) 상태에서 F5 새로고침 시 수집이 자동 실행되던 트리거 버그 수정.
  - F5 새로고침 접속 시 항상 DB 파일(`articles_db.json`)을 즉시 0.01초 만에 불러오도록 고쳐 수집이 재실행되지 않도록 완벽 보장.

### v1.7.6 (2026-08-08) - UI 수집 버튼 연속 중복 클릭 방지 (isRefreshingArticles 가드 적용)
- 🔒 **수집 연쇄 폭주 방지 (`public/app.js`)**:
  - `[🔄 수집 / 새로고침]` 버튼을 수 초 내에 연달아 누를 때 수집 요청이 중복 발송되어 Cloudflare 503을 유발하던 현상 완벽 차단.
  - 수집 진행 중 중복 클릭 시 `⏳ 이미 최신 뉴스 수집이 진행 중입니다` 안내 토스트 출력 및 무시 처리.

### v1.7.5 (2026-08-08) - Bing 백업 보장 및 rss2json 429 예외 안전 처리
- 🛡️ **Bing 3차 백업 무조건 성공 보장 (`scraper.js`)**:
  - Bing 백업 시 필터 조건으로 인해 0건이 반환되어 4차 rss2json으로 넘어가던 예외 방지.
  - 최신 기사 최소 2건 이상 확보 전까지는 기존 기사 포함 상위 최신건을 무조건 반환하여 3차 Bing 단계에서 수집 완료 보장.
- 🚫 **rss2json 429 에러 안전 방어 (`scraper.js`)**:
  - rss2json 무료 API 한도 초과(429 Too Many Requests) 시 런타임 수집 실패 로그 대신 안전하게 소프트 캐칭.

### v1.7.4 (2026-08-08) - 원본 기사 사진 4단계 고화질 자동 추적·추출 엔진 전격 개편
- 🖼️ **기사 사진 추출 실패 완벽 해결 (`server.js`)**:
  - 기존 구글 리다이렉트 링크 처리 누락 및 로고/JS 파일 오인식으로 인한 "대표 이미지를 찾을 수 없습니다" 버그 수정.
  - **1단계**: 구글 우회 링크 자동 해독(Redirect Resolution) 후 원본 언론사 `og:image` 및 `twitter:image` 정밀 추출.
  - **2단계**: 네이트판/블라인드/뉴스 본문 내 실제 첨부 사진(`fimg`, `download.jsp`, `imgnews`) 직접 추적.
  - **3단계**: 네이버 뉴스 포털 고화질 포토 검색 (로고/스크립트 자동 제거).
  - **4단계**: 다음(Daum) 뉴스 포털 포토 백업 검색 릴레이.

### v1.7.3 (2026-08-08) - DB 5일 보관 룰 원복 및 2시간 이내 수집 필터 유지
- 💾 **DB 보관 기간 5일 보장 (`history.js`)**:
  - 수집된 기사는 IT/금융/커뮤니티 카테고리에 상관없이 DB에 **5일 동안 안전하게 보관**되도록 원복.
- 🎯 **수집 시점 2시간 이내 필터 유지 (`scraper.js`)**:
  - 새로 수집하는 시점에는 IT, 코인, 주식, 경제 카테고리에 대해 최근 2시간 이내(`when:2h`) 발행된 신규 기사만 수집하여 DB에 축적.

### v1.7.2 (2026-08-08) - 네이트판 실제 게시시간 파싱 & IT/금융 2시간 규격(when:2h) 엄격 필터 원복
- ⚖️ **네이트판 실제 작성일시 추출 (`scraper.js`)**:
  - 기존 `new Date().toISOString()` 고정값으로 인해 모든 글이 '1분 전'으로 표시되던 버그를 완벽 수정.
  - 네이트판 본문 HTML(`fetchNatePannDetail`)에서 실제 작성일시(`YYYY.MM.DD HH:mm`)를 정규식으로 파싱하여 정확한 게시시간 반영.
- 💻 **IT/코인/주식/경제 2시간 이내(`when:2h`) 엄격 필터링 (`scraper.js`, `history.js`)**:
  - Bing 백업 엔진 수집 및 DB 저장 시 2.5시간을 초과한 1~2일 전 구 기사가 섞여 들어가던 현상 완벽 제거.
  - IT, 코인, 주식, 경제, 글로벌 외신 카테고리는 최근 2.5시간 이내 최신 기사만 수집 및 DB 보관되도록 엄격 필터링 적용.

### v1.7.1 (2026-08-08) - Cloudflare Worker 503 동시성 제한 방지 (120ms Pacing 적용)
- 🚀 **Cloudflare HTTP 503 Service Unavailable 완벽 해결**:
  - `Promise.all`로 13개 요청이 동일 마이크로초에 한 번에 몰려 Cloudflare Worker 차단(HTTP 503)되던 동시성 한계를 120ms 순연 인터벌(Pacing)로 전격 개선.
  - 전 카테고리 100% Cloudflare 구글 원본 수집(`🚀 [2차 Cloudflare 구글 성공]`) 유지 및 전체 수집 속도 ~2초 초고속 유지.
  - `fetchNatePannArticles` 내 미정의 변수 `seenLinks` 오류 수정.

### v1.7.0 (2026-08-08) - 13개 카테고리 풀 병렬(Promise.all) 초고속 수집 최적화 (~1.5초 달성)
- 🚀 **수집 속도 20배 향상 (`45초 ➔ ~1.5초`)**:
  - 기존 13개 카테고리 순차 수집(`await` 순연)을 `Promise.all` 동시 병렬 수집으로 전격 개편.
  - 네이트판 기사 본문 추출(`fetchNatePannArticles`) 내부 순차 Loop를 `Promise.all` 병렬 추출로 개선하여 16초 지연 제거.
  - Render 서버 구글 직통 타임아웃을 `1200ms`로 최적화하여 IP 차단 감지 즉시 0.1초 만에 Cloudflare Worker로 넘어가도록 개선.
  - Cloudflare Worker 사전 예열(Warmup Preflight) 및 타임아웃 10초 상향으로 콜드 스타트 지연 완벽 해결.

### v1.6.0 (2026-08-08) - Cloudflare Workers 구글 뉴스 원본 우회 엔진 탑재
- 🚀 **Cloudflare Worker 100% 무료 구글 원본 우회 프록시 연동 (`scraper.js`)**:
  - `CF_WORKER_URL` (`https://bold-morning-65d1.la5454la.workers.dev/`) 전격 연동.
  - **1차 시도**: 구글 뉴스 RSS 직통 (노트북 환경)
  - **2차 시도**: Render 클라우드 서버에서 구글 직통 차단 시 **Cloudflare Worker를 통해 구글 뉴스 원본 100개 풀 수집** (`⚡ [구글 원본 프록시 성공]` 로그 기록)
  - **3차 시도**: Bing News RSS 백업 엔진
  - **4차 시도**: rss2json 백업 릴레이
  - Render 서버에서도 구글 뉴스 원본 기사 100개 풀 수집 100% 보장!

### v1.5.0 (2026-08-08) - Bing News RSS 하이브리드 백업 수집 엔진 탑재
- 🚀 **Bing News RSS 2차 백업 수집 엔진 추가 (`scraper.js`)**:
  - **1차 시도**: 구글 뉴스 RSS 직통 (5초 타임아웃) ➡️ 허용 IP(로컬 컴퓨터 등)에서는 구글 원본 100개 풀 수집!
  - **2차 시도**: 구글 직통 차단 시(Render 서버 등 클라우드 IP) **Bing News RSS 백업 엔진으로 자동 전환** (`parseBingNewsXml` 적용, `🔄 [Bing 백업 수집 성공]` 로그 기록)
  - **3차 시도**: rss2json 백업 릴레이
  - Render 서버 24시간 자동 실행 환경에서도 IP 차단 없이 IT, 코인, 주식, 경제, 글로벌 외신, 커뮤니티 기사 수집 100% 보장!
- ⚡ **검색 쿼리 및 로그 최적화**:
  - Bing News RSS 검색 연동을 위한 불리언 키워드 쿼리 정제 (`OR` 및 가로 특수문자 자동 변환)
  - `addLog` 시스템 로그에 `⚡ [구글 직통 성공]`, `🔄 [Bing 백업 수집 성공]`, `🚫 [구글 직통 차단/지연 ➔ Bing 백업 전환]` 명확히 구분하여 실시간 기록.

### v1.4.0 (2026-08-07) - 스마트 하이브리드 수집 엔진
- 🧠 **1차 직통 ➔ 2차 우회 자동 전환 하이브리드 수집 엔진 적용**:
  - **1차 시도**: 구글 뉴스 RSS **직통 연결 (3.5초 타임아웃)** ➡️ 차단 안 당했으면 구글 원본 100개 풀 수집!
  - **2차 시도**: 구글 직통 실패/차단 시 **자동으로 `rss2json` 우회 통로로 자동 백업 전환** (`⚠️ [구글 직통 제한 ➔ 우회 전환]` 로그 기록)
  - 렌더 서버 IP가 안 막혔을 땐 100개 풀 수집, 막혔을 땐 백업 우회로 0초 멈춤 보장!

### v1.3.0 (2026-08-07)
- 🌟 **신규 모드 추가**: 버전 7 `하이브리드 후킹 (전문가 뷰)` 추가 (본문 전체 1단 + URL 링크 2단 분리 적용)
- ✏️ **명칭 변경**: 버전 2 1인칭 후킹 ➡️ `아티클 (1인칭 뷰)`로 명칭 변경
- 🚨 **로그 시스템 강화**: IP 차단(403/429), 타임아웃 발생 시 `addLog` 시스템에 실시간 붉은색 경고 로그 남기도록 수정
- ⚡ **성능 및 수집 원복**: 
  - Render 504 Gateway Timeout 방지를 위해 `Promise.all` 병렬 수집 유지
  - 사용자 지정 수집 정책 준수: 테크/금융 `2h` (2시간 이내), 커뮤니티 `5d` (5일 이내) 재확인 및 고정

### v1.2.0 (2026-08-07)
- Render 무료 호스팅 IP 차단 우회를 위한 `rss2json` API 프록시 적용

### v1.1.0 (2026-08-06)
- 네이트판, 블라인드 수집 키워드 최적화 및 캔버스 이미지 크기 자동 맞춤 패치

---

## 📌 6. 기사 확인(isRead) 상태 관리 메커니즘 (Strict State Directive)

1. **상태 전환 트리거 (State Mutation Triggers)**:
   - **단건 확인 (`isRead = true`)**:
     - 기사의 `[✨ 트윗 생성]` 버튼 클릭 시
     - 기사의 `[원문보기 ↗]` 링크 클릭 시
   - **일괄 확인 (`markAllAsRead`)**:
     - 상단 툴바의 **`[모두 읽음]`** 버튼(`btn-mark-all-read`) 클릭 시에만 전격 실행
2. **대시보드 상단 미확인 카운트 카드의 역할 (UI Filtering Only)**:
   - 대시보드 상단의 **`[데크/금융 미확인 X건 (클릭)]`**, **`[커뮤니티 미확인 Y건 (클릭)]`** 상자는 기사 상태를 읽음으로 바꾸는 변경 버튼이 **아니라**, 화면 목록을 미확인 기사만 골라 보는 **`필터링 토글 버튼`**임 (`selectCategoryNav(cat, true)`).
   - 따라서 카드를 클릭하더라도 기사의 `isRead` 상태 데이터는 절대 변하지 않음.\n
